# 跨平台复现守门（macOS <-> Windows）
#
# 背景：Git for Windows 默认 core.autocrlf=true，会在 checkout 时把 LF 转成 CRLF。
# 文本文件的行尾不一致会让 gate_lint / diff 出现噪声；二进制文件若被误判为文本，
# 换行转换会直接损坏内容（BAM/gz 会变成打不开的坏文件）。
#
# 本项目入库的二进制共 152 个（gz 7.8MB / fq 6.3MB / bam 4.3MB / png 3.9MB / fa 1.2MB /
# ht2 8 个），此前全靠 git 的启发式判定，此处改为显式声明。

# 默认：文本一律按 LF 归一（工作区与版本库都是 LF，两台机器一致）
* text=auto eol=lf

# 明确为文本的源码/文档
*.md    text eol=lf
*.py    text eol=lf
*.txt   text eol=lf
*.sh    text eol=lf
*.json  text eol=lf
*.tab   text eol=lf
*.log   text eol=lf
*.out   text eol=lf
*.yml   text eol=lf
*.yaml  text eol=lf

# 二进制：禁止换行转换，禁止 diff 尝试按文本处理
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
*.pdf   binary
*.zip   binary

# 生信二进制格式
*.bam   binary
*.bai   binary
*.cram  binary
*.crai  binary
*.sam   text eol=lf
*.fq    binary
*.fastq binary
*.fa    binary
*.fasta binary
*.gz    binary
*.bz2   binary
*.tbi   binary
*.csi   binary
*.bed   text eol=lf
*.ht2   binary
