# 2026-08-28 工作日志

## 进度确认（接续 8/26「先 A 入库 004/005/006 → 再开 007」建议后的实际推进）
- 8/26–8/27 实际推进远超预期，alignment 与 read-alignment 两大家族已闭环：
  - alignment 分支 7 skill 全入库：004 pairwise / 005 msa-statistics / 006 trimming / 007 multiple-alignment(MAFFT L-INS-i) / 008 msa-parsing / 009 alignment-io / 010 structural-alignment(Superimposer/TMalign/Foldseek)。
  - read-alignment 分支 4 skill 全入库：011 bowtie2 / 012 bwa / 013 hisat2 / 014 star。
  - 站点接入 DEEP DIVE 01–10 + read-alignment；小红书正文/卡片体系（md2card 出图、gate_lint 标题≤20字）已落地。
- 结论：004–014 全在版本库，alignment + read-alignment 序列比对主题闭环。

## 工作区清理（本次收尾）
- 残留：`M memory/2026-08-27.md`、`?? overview_009.md`、`?? content/库/`（实为 md2card 外部 clone 漏忽略）、`?? .workbuddy/artifacts/`。
- 修正 `.gitignore`：补 `content/库/md2card/`（含 .git，属外部 clone，与 bioSkills/obsidian-wechat-converter 同列）+ `.workbuddy/artifacts/`（临时产物）。
- 提交 `.gitignore` + `memory/2026-08-27.md` + `overview_009.md`（commit 6c2c294）。提交后 `git status` 干净。

## 下一步（S1 选题，待定方向）
- bioSkills 仓库尚有 80+ 家族未做（variant-calling / differential-expression / single-cell / chip-seq / atac-seq / genome-assembly / phylogenetics / metagenomics …）。
- 候选下一目标：**variant-calling**（序列比对最直接下游，NGS 流程连贯，生信核心热门）。需用户认可或指定其他方向，再开 S2a 真跑。

## 015 variant-calling 完成 (23:50)
- S2a 真跑：samtools 官方 mpileup 测试夹具(mpileup.1.bam + mpileup.ref.fa,
  chr17:1-4200), bcftools 1.24 标准命令检出 **9 变异**(8 SNP + 1 INDEL), 全 PASS。
  验证了 -q/-Q 过滤(高 MAPQ 数据 no-op)、norm 步骤、同组 BAM 一致性(.2→7, .3→10)。
- S2b 笔记：深拆版 `015-bioSkills真实试用-variant-calling.md` +
  精简版 `015-bioSkills小红书-variant-calling-正文.md`，均入库。
- S3 站点：build_lab_site.py 新增 variant-calling 分类页 + 015 深度页，
  DONE 集合与 treemap 同步。commit `369889c`。
- 数据源解决：bcftools test/ 无 BAM；htslib raw URL 返回 404；
  最终从 samtools/test/mpileup/ 下载官方测试夹具（真实二进制）。
- 出图 3 张：类型构成 / 等位基因深度 / 位置轨迹
  (brick-red #b5482f + celadon #2f7d72)。
- **进度：004–015 全部完成入库**，alignment(7) + read-alignment(4) + variant-calling(1)。

## 分类层级重排 + 站点配置化 (14:00 完成)
- 用户确认：复用 bioSkills 分类名 + 现在就重排（不预建空目录、不跨领域铺开）。
- 三处 git mv/ mv：笔记/素材 用 git mv（保留历史），xhs-cards 用 mv（output 不进库）。
  - 笔记/素材进 alignment(004-010) / read-alignment(011-014) / variant-calling(015)；
    001-003 早期杂项保持扁平。
- 关键 bug 修复：笔记图片引用改为 ../../素材/<cat>/ 后，load_note 仅当源文件存在才
  改写+收集（assets 曾为空）；修正后 assets/ 收 15 图、HTML img src 全改 ../assets/、无坏路径。
- build_lab_site.py 配置化：删 IMG/EXP 死代码，TRIALS + cat_page() 自动扫描，
  新增一篇只加 TRIALS 一行。
- 发布脚本 md2card_automation.py / trial2xhs.py：从 md 路径推导 cat 层，
  产物 output/xhs-cards/<cat>/<slug>/，素材根退两层到 content/素材/<cat>/。
- README 目录树/索引表(004-015)/命名规范/新增流程 全更新为分类结构。
- 提交 5658729（716 files, +165/-181）。
