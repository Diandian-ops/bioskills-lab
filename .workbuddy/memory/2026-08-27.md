# 2026-08-27 工作日志

## 007 bioSkills multiple-alignment 真实试用（完成，已推送 c4e7fa5）
按 004/005/006 既定模式走下一个 alignment skill（multiple-alignment，构建 MSA，主工具 MAFFT）。

- 真实试用：brew 装 MAFFT 7.526；用仓库自带 `sequence-io/read-sequences/examples/sample.fasta`（3 条合成序列，非同源）严格复现 SKILL.md 推荐 L-INS-i（`--localpair --maxiterate 1000`）及仓库 `run_msa.py`。实测 102 列、44 无 gap 列（43.1%）、整体 gap 35.3%；两种调用产物逐字节一致。
- 产物：`content/笔记/007-bioSkills真实试用-multiple-alignment.md`（深拆版，进站点）+ `*-小红书-multiple-alignment-正文.md`（正文，标题「MAFFT别用auto 超200就降级」19字）+ `content/素材/007-multiple-alignment/`（fasta/log + gap 分布图 007-fig.png）。
- 出图：trial2xhs.py 生成 notebook-standalone-hd 12 张 PNG（output/ 已 gitignore）。
- 已 `git push origin main`（c4e7fa5），工作树干净。

## 关键经验（可复用）
- **gate_lint 有两种模式**：`*-正文.md` / 含 `-小红书-` → 正文严格模式；其余（真实试用深拆笔记）→ 笔记(真实试用/通用)模式。**笔记模式更严**，会把「陷阱」「天花板」等词判为 METAPHOR（ERROR）。本次 007 笔记初稿因此 3 处 ERROR，改「降级陷阱→静默降级」「依赖天花板→依赖上限」后通过。写深拆版笔记时避开隐喻词。
- **工具未装时的真实试用**：multiple-alignment 的 CLI（mafft/muscle/clustalo/t_coffee）本机均未预装，用 `brew install mafft` 装好即跑通。其余未装工具（MUSCLE5/ClustalOmega/T-Coffee/PAL2NAL）的成分拆解按纪律只做忠实转述、不编造结果。
- **仓库自带示例局限**：alignment 类 skill 的 examples 多为 .py 脚本，FASTA 输入样本稀缺（`sample.fasta` 为合成非同源序列）。用它做"工具链路跑通"验证没问题，但高 gap 比例恰好印证 skill 自身「非同源序列不应做 MSA」警示——该点已在笔记客观写明，未粉饰。

## 复核（用户要求，08:30 完成）
- 事实层：笔记关键声明 5 项逐条对回 SKILL.md 原文全对（--auto 四档阈值/MUSCLE5 16=4×4/GUIDANCE2 0.93 按 MAFFT-LINSI 标定/Biopython1.86 移除/200 条翻转）。
- 实测数字独立重算一致（102 列/44 无 gap/35.3%）；两份 lint 复跑均 0 ERROR 0 WARN。
- **发现并修复一个真缺口**：007 提交时只提交了笔记，未重建站点 → build_lab_site.py 补 7 处接线（NOTE007 加载/写出、ALIGN_SUBS、侧栏 active×2、skip-list、总览 sklist "DONE 007"/"已完成 4 个"），重跑构建 + skill 校验清单全过（figure 包图、http 200×3），已推送 e2bea47。
- **流程教训：trial2xhs 流水线不含站点构建步**——S3 站点重建是独立步骤，新笔记提交后必须手动跑 build_lab_site.py。后续 00X 笔记 checklist 应加"重建站点"一项。

## 待办 / 备注
- 005 标题「差出2.8倍」与正文 3.44/6.23 bits 口径仍待用户确认（2.8 可能是 bits 差而非倍），用户尚未回复。
- 用户级 skill `redbook-xhs-body` 此前已同步至 workbuddy-skills（fe9c34c），本次未改动 skill。
- 下一个候选 skill：alignment 专题剩 alignment-io / msa-parsing / structural-alignment，或继续按叙事推进。

## 008 bioSkills msa-parsing 真实试用（完成，已推送 b1e9786）
- 真实试用：纯 Biopython（AlignIO）。用仓库自带 `alignment-io/examples/sample_alignment.aln`（CLUSTAL，4×21）严格复现 SKILL.md 全部函数：保守列(18/21 完全保守)/gap(col12 为 3/4 序列 gap 列)/共识(ATGGCTAGCTAGNACGTACGT，col12→N)/过滤/坐标映射/Henikoff/Neff/MI-APC。
- 关键发现（如实写进笔记）：加权指标在小样本上退化——Henikoff 全 0.25、Neff=0.25（4 条聚 1 簇，Neff/L=0.012）、MI 全 0，正好对应 skill 标注的适用边界。用 007 真实 102×3 MAFFT 对齐二次验证：Henikoff 0.31/0.31/0.38（非均匀）、50 gappy 列 → 同一套函数在真实数据上正常分化。
- 产物：深拆版 `008-bioSkills真实试用-msa-parsing.md`（进站点 DEEP DIVE 05）+ `*-小红书-msa-parsing-正文.md`（标题「小样本上多序列比对加权会退化」14字）+ `content/素材/008-msa-parsing/`（run_msa_parsing.py/make_fig.py/008-fig.png/日志/样本）。
- 两份 lint 0/0；build_lab_site.py 接线 008（ALIGN_SUBS 加 msaparse / 侧栏 active+skip / 总览 DONE 008 / 已完成 5 个），重建站点 http 200 抽检全过。
- 提交 b1e9786（8 文件，668 行）。output/ 仍 gitignore，站点本地生成。
- **出图阻塞**：trial2xhs 两道机检过、md2card 5173 在线，但导出对话框提交按钮 30s 内未解除禁用（md2card dev server 共享实例渲染/preflight 态卡住；007 同流程成功）。属外部环境问题，未擅自重启用户 5173 服务。服务恢复后重试：`python pipeline/trial2xhs.py content/笔记/008-bioSkills真实试用-msa-parsing.md`。其余 alignment 候选剩 alignment-io / structural-alignment（后者需 Foldseek/TM-align，成本更高）。

## 009 bioSkills alignment-io 真实试用（完成，已推送 641e154）
- 真实试用：纯 Biopython（AlignIO/Bio.Align）。复用仓库 `alignment-io/examples/sample_alignment.aln`（CLUSTAL，4×21）严格复现 SKILL.md 12 个文档化模式：读/写/转换/切片/程序化构建/PHYLIP 严格截断/Stockholm 注释保留/A2M 大小写提取/Bio.Align 现代 API/MAF 坐标转换/pyhmmer 流式声明。
- 关键发现（如实写进笔记）：
  1. PHYLIP strict 截断仍发生，但 Bio 1.88 对冲突长名直接抛 ValueError（非静默合并），phylip-relaxed 可保留长名；
  2. Stockholm→FASTA/Clustal/PHYLIP-relaxed 全部静默丢 GS/GR/GC，NEXUS 仅保留 GS， empirically 确认注释独占性；
  3. MAF `row.annotations['strand']` 在 Bio 1.88 中是整数 -1/1，SKILL.md 示例按字符串 '-' 判断会导致负链 plus_start 错误（10 bp 而非正确 33 bp），给出兼容整数/字符串的修正版。
- 产物：深拆版 `009-bioSkills真实试用-alignment-io.md`（进站点 DEEP DIVE 06）+ `*-小红书-alignment-io-正文.md`（标题建议「比对格式转换会静默丢注释」12字）+ `content/素材/009-alignment-io/`（run_alignment_io.py/make_fig.py/009-fig.png/annotation_survival.json/日志/样本）。
- 两份 lint 0/0；build_lab_site.py 接线 009（ALIGN_SUBS 加 alnio / 侧栏 active+skip / 总览 DONE 009 / 已完成 6 个），重建站点 http 200 抽检全过。
- 输出/站点仍 gitignore，本次提交范围：笔记×2 + 素材目录 + build_lab_site.py + 工作日志。

## 010 bioSkills structural-alignment 真实试用（完成，已推送 9c99f7f / f2467b2）
- 真实试用（三个外部工具全真跑）：
  1. **Bio.PDB.Superimposer**：1ubq↔1ubi（同属泛素）RMSD=0.0908 Å（76 CA，几乎重合）。
  2. **TM-align**（源码编译 v20220412，macOS 修 `<malloc.h>`→`<malloc/malloc.h>`）：1ubq↔1ubi TM=0.9991/RMSD0.09（>0.5 同折叠）；1ubq↔1fmb TM=0.4079/RMSD3.41（<0.5 异折叠）。
  3. **Foldseek**（conda env `foldseek` v10.941cd33，`easy-cluster` 把 1ubq+1ubi 聚一簇、1fmb 单独；`easy-search` 默认过滤 TM<0.5 跨折叠候选）。真实 PDB 从 RCSB 下载 1ubq/1ubi/1fmb。
- 关键发现（如实写进笔记）：
  1. SKILL.md `--format-output` 里 `tmscore` 在 Foldseek 实际不可用，正确为 `alntmscore`/`qtmscore`/`ttmscore`（已实测验证），属 P2 小修正，未自动改原 skill；
  2. Foldseek 默认 3Di+AA 检索口径会漏掉低相似度跨折叠候选，低相似探测需 `--alignment-type 1` 或 `easy-cluster` 复核；
  3. US-align/DALI/Foldmason/Foldseek-Multimer/pLDDT 掩码/PyMOL·ChimeraX 可视化 仅按文档转述、未实测。
- 产物：深拆版 `010-bioSkills真实试用-structural-alignment.md`（进站点 DEEP DIVE 07）+ `*-小红书-structural-alignment-正文.md`（标题「结构比对工具实测：TM-align+Foldseek」16字）+ `content/素材/010-structural-alignment/`（run_structural_alignment.py/make_fig.py/010-fig.png/structural_results.json/TMalign 二进制/pdbs/日志）。
- 两份 lint 0/0；build_lab_site.py 接线 010（ALIGN_SUBS 加 sa / 侧栏 active+skip / 总览 DONE 010 / 已完成 7 个），重建站点 http 200 抽检全过（DEEP DIVE 07、TM-score 0.4079、已完成 7 个）。
- **提交**：先建 commit 9c99f7f（本地成功），首次 push 因 `github.com:443` 连接失败两次受阻；网络恢复后（本会话 11:16）retry push 成功（`641e154..9c99f7f`），随后修正 overview 标注再推 f2467b2（`9c99f7f..f2467b2`）。**至此 alignment 家族 7 个 skill（004–010）全部完成并上线 origin/main。**
- 遗留未跟踪（已核查）：`content/素材/` 整棵已随 010 commit 全跟踪，008 残留临时文件不存在；实际仅 `overview_008.md`（008 遗落 artifact）未跟踪，已在本会话补提交 `9555f4a`。

## 008 出图（本会话 11:25 处理完成）
- 网络恢复后 retry `trial2xhs.py 008-bioSkills真实试用-msa-parsing.md` 仍失败：md2card 导出对话框 `.dialog-submit` 按钮 60s 始终 `disabled`，无 console 报错（非网络、非 JS 异常）。
- **根因定位**：写隔离诊断脚本轮询 + 抓取 console，确认 preflight 静默卡死。对比发现——008 真实试用版 md 引用 `008-fig.png`，md2card 绑图后导出 preflight 等待图片 ready 永远不置位；改用**正文版**（不引用图片）走同一流程 submit 正常解除 disabled，成功导出 2 张 PNG（`output/xhs-cards/008-bioSkills小红书-msa-parsing-正文/notebook-standalone-hd/`）。属 md2card 应用层图片绑图 preflight bug，非本机问题。
- 处理：清理真实试用版尝试留下的空目录 `output/xhs-cards/008-bioSkills真实试用-msa-parsing/`；`overview_008.md` 阻塞项段改写为出图完成说明；补提交 `9555f4a` 并推送。
- **待用户决定**：若需真实试用版含图卡片，须修 md2card 图片 preflight 或绕过绑图（未自行改第三方应用）。

## 009 出图（本会话 11:34 完成）
- 按 008 同法：`trial2xhs.py 009-bioSkills小红书-alignment-io-正文.md` 成功，导出 2 张 PNG → `output/xhs-cards/009-bioSkills小红书-alignment-io-正文/notebook-standalone-hd/`（正文版不引用图片，未触发绑图 preflight 卡死）。
- 现状小结（alignment 7 个 skill 小红书出图）：004/007 真实试用版含图出图成功；008/009 真实试用版因图片绑图 preflight 卡死（008 已诊断确认），均改用正文版出图；005/006 真实试用版 `output/xhs-cards` 目录为空（出图未成功，原因待核，疑同类）；010 尚未出图。
- **根因更正（重要）**：此前多处置"图片绑图 preflight 卡死"为误诊。本会话精确诊断（读 `preflight.status` + 每张 img 状态）确认：图片加载完全正常（`complete:true, naturalWidth:1260`）；真实阻断是 `useExportWorkflow.ts` 的**卡片内容溢出门禁**——008/009 真实试用版（长文+图）分页后单卡 `.card-content` 溢出（`scrollHeight>clientHeight+1`）被 `throw` 成 preflight error，submit 因而 disabled。004/007 真实试用版不溢出故成功；正文版短故不溢出。

## read-alignment 家族开启（011–014，专题02，接续 alignment 专题）
- 工具环境：`conda create -n bioaligners -c bioconda -c conda-forge bowtie2 bwa hisat2 bwa-mem2`（bowtie2/bwa/hisat2 已装；bwa-mem2 本会话补装）。samtools 用本机 anaconda 1.21。
- 测试数据范式：合成参考 `gen_ref.py`（固定种子，4×3000bp=12kb）+ `wgsim -N 3000 -e 0.02` 造 PE reads（自带 wgsim）；适配器污染对照用 R1 3' 拼 20bp TruSeq adapter。

## 011 bowtie2-alignment 真实试用（完成，已推送 9b513ca）
- 真实复现（全真跑）：bowtie2-build 建索引(6个.bt2)；基础 end-to-end 比对率 99.23%、max MAPQ=42（印证 e2e 上限 42）；适配器污染 e2e 49.28% vs --local 99.77%（**+50.49pp**，local max MAPQ=44）；ChIP 旗标报告率 50.93%（合成 4 碎片参考跨 contig 配对被 --no-discordant 丢，真实染色体不发生，笔记如实标注）；ATAC 旗标 100%、MAPQ44；预设 very-fast/sensitive/very-sensitive=95.95/99.23/99.98；`-x` 传文件名 rc=255 Exiting now。
- 产物：深拆版 `011-bioSkills真实试用-bowtie2-alignment.md`（DEEP DIVE 08）+ `*-小红书-bowtie2-alignment-正文.md`（标题「bowtie2 比对模式选错掉一半」18字）+ `content/素材/011-bowtie2-alignment/`（run_bowtie2.py/make_fig.py/011-fig.png/bowtie2_results.json/参考+reads+bams+索引+日志）。
- 两份 lint 0/0（仅 WARN：序列号"011"前缀 + MAPQ 数值带上下文，与 008/009/010 同惯例）；011 小红书正文卡片(2 PNG) + 真实试用版含图卡片(10 PNG) 均已出（md2card 溢出降级修复生效）；已 commit+push 9b513ca。

## md2card 出图根因修复（本会话 11:40，选项 B）
- 改 `content/库/md2card/src/app/hooks/useExportWorkflow.ts`：将"内容溢出"从硬阻断（`throw`→error 阻止导出）降级为**告警但仍允许导出**（置 preflight `ready`，message 提示 N 张卡片可能溢出/裁切）。markdown 解析错误的硬阻断保留不动。
- 验证：008 真实试用版（含图）导出 **12 张 PNG** 成功；005/006/009/010 真实试用版均成功（各 10–13 张）。至此 alignment 7 个 skill 真实试用版含图卡片全部生成（004:11/005:10/006:11/007:12/008:12/009:13/010:10；另 008/009 正文版各 2 张）。
- **注意**：该修复是 md2card 全局行为变更（溢出不再阻止导出，可能裁切）。`content/库/md2card/` 在 RedBook 仓库中**未跟踪**（git status `??`），改动仅本地生效，未进 RedBook commit；若需永久保留须单独提交到 md2card 自身仓库（若有）。小红书发布仍建议用正文版（短、不溢出、不裁切）。**用户 11:49 确认：不提交 md2card 改动（既不进 RedBook 也不进 md2card 自身仓库），仅本地生效。**

## 012 bwa-alignment 真实试用（完成，已推送 2dd59e9）
- 真实复现：bwa index 建 SA-index（bwa 原版）；`@RG` 硬契约（带 `-R` BAM 含 @RG、缺 `-R` 则无）；max MAPQ=60（与 011 bowtie2 的 42/44 形成标度对照）；`-K` 两次跑 md5 一致（可复现）；bwa 原版索引喂 bwa-mem2 直接报错（索引不互通）；`-M` vs `-Y` SV 语义（合成数据无对应场景，按文档转述未实测）。
- 产物：深拆版 `012-bioSkills真实试用-bwa-alignment.md`（DEEP DIVE 09）+ 小红书正文（标题「bwa 比对前先定 @RG 契约」）+ `content/素材/012-bwa-alignment/`。两份 lint 0/0；卡片(2+10 PNG) 已出。已 commit+push 2dd59e9。

## 013 hisat2-alignment 真实试用（完成，已推送 ba849a4）
- 真实复现：手工跨 junction 读长（外显子1尾+内含子+外显子2头）被对齐为 `50M800N50M`（剪接能力直接可见）；两趟法发现并复用 1 个 novel junction；max MAPQ=60（与 bwa 同台、GATK 友好标度）；`-x` 传文件名 rc=255；首跑因 FASTA 输入漏 `-f` 失败、修正后即命中（脚本输入格式问题，非工具缺陷）。XS 标签/链特异性按建库化学设。
- 产物：深拆版 `013-bioSkills真实试用-hisat2-alignment.md`（DEEP DIVE 10）+ 小红书正文（标题「hisat2 跨内含子才看得到剪接」）+ `content/素材/013-hisat2-alignment/`。两份 lint 0/0；卡片已出。已 commit+push ba849a4。

## 014 star-alignment 真实试用（完成，已推送 27b3b71 / a1f6496）
- **关键环境限制（诚实交付）**：STAR 比对步骤在本沙箱无法运行——STAR 对所有 reads 输入（FASTA/FASTQ/FIFO、toy/真实、工程或 /tmp、沙箱开/关、STAR/STARlong）均输出 `Number of input reads | 0` + `nextChar=-1`；DYLD interpose 追踪确认 STAR **从不打开 reads 文件**直接判 0，而同一文件 C/C++/Python mmap 正常。属环境级不兼容，非参数/路径/垫片可修。genomeGenerate 阶段（走 ifstream）真实跑通（rc=0，18 索引文件，sjdbOverhang=99，genomeSAindexNbases=5）。
- 产物：深拆版 `014-bioSkills真实试用-star-alignment.md`（DEEP DIVE 11，status=partial-real，比对步骤标注 env-blocked 未编造）+ 小红书正文 `*-star-alignment-正文.md`（标题「STAR 比对 RNA 别漏3点」9字，纠正 METAPHOR「踩坑」为客观表述）+ `content/素材/014-star-alignment/`（gen_ref.py/run_star.py/make_fig.py/star_index(18文件)/star_results.json/014-fig.png）。
- 两份 lint：深拆 0/0、正文 ERROR=0 WARN=3（序列号/日期 BARE_NUM，同惯例）。
- 卡片：trial2xhs 生成 深拆版 10 PNG + 正文版 2 PNG（output/ 已 gitignore）。
- **站点接入（read-alignment 家族，task 52）**：`build_lab_site.py` 扩展——DONE 加 read-alignment；新增 `READ_ALIGN_SUBS` + 通用 `FAMILY_SUBS`/`FAMILY_KEYS`（对齐/读取两家族共用子页机制，去 011 前逐族硬编码）；侧栏/概览/首页统计动态化；`skill_page` 增 `cat` 参数修面包屑；load NOTE011-014 + 生成 read-alignment/{index,bowtie2,bwa,hisat2,star}.html；为 011-014 笔记补 `![...](../素材/.../0XX-fig.png)` 内联配图引用（对齐 010 约定，供站点渲染）。重建站点 http 200 抽检全过（DEEP DIVE 11、014-fig.png 绑定、read-alignment 链接就位）。
- **提交**：分 3 个 commit 推送 origin/main：`27b3b71`（011-014 笔记内联配图引用，含 014 深拆笔记新建）、`a25e...`（014 素材+正文笔记）、`a1f6496`（build_lab_site.py 接入 read-alignment 家族）。push `ba849a4..a1f6496`。
- **纪律延续**：STAR 比对实测数字全部按 SKILL.md 文档转述、未编造；站点 output/ 仍 gitignore（CI 从 content/ + pipeline/ 重建）。
- 待用户决定：`overview_009.md` 冗余 artifact 仍未跟踪（按纪律不擅自动）。

## bioaligners conda 环境（后台安装任务收尾，本会话末确认）
- 三个后台 conda 安装任务（t2QRWS 建 env+bowtie2/bwa/hisat2；f1SjZC 加 bwa-mem2；SSvbHc 加 star）已全部完成。核验：`/Applications/anaconda3/envs/bioaligners/bin/` 下 bowtie2/bwa/hisat2/bwa-mem2/STAR(2.7.11b)/STARlong 全 OK；samtools 不在该 env，沿用 base anaconda(`/Applications/anaconda3/bin/`)绝对路径。
- 含义：read-alignment 家族所需比对器已全量就绪，后续 015+ 试用无需再装环境。STAR 比对步骤沙箱失效问题（见 014）与安装无关，仍属环境级 reads 通道不兼容。
