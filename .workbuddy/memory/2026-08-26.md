# 2026-08-26 工作日志

## 004/005 按新 skill 纪律补齐（用户质疑"004 是不是没用新 skills 优化"）
- **核查结论**：004/005 是早期产物，前几轮只改了"真实试用"前缀，其余新规则（去编号标签、封面白底、去副标题、去防御性表述）未系统应用。
- **004 站内版**：`**坑一：...**`/`**坑二：...**` → 去编号；`### 坑一实测/坑二实测` → 描述性标题（默认 gap=0 陷阱 / Percent Identity 四口径）。
- **004 小红书版**：`## 坑一/坑二` → 描述性标题；`我拿真实数据测了` → `怎么跑的`。
- **004/005 封面**：旧暖纸底 #eee8db（make_cover.py 旧版生成）未重生成 → 用新白底 make_cover.py 重生成 004/005-cover.png。
- **005 站内版**：`**坑一/坑二：...**` + `### 坑一实测` → 去编号/描述性标题。
- **005 小红书版**：`我拿真实数据测了` → `怎么跑的`。
- 004/005 出图脚本（make_figs.py）经核查**已合规**（legend 右外、底部 caption、语义配色、dpi150）——本次无需改。
- **全量核验**：`grep` 6 篇笔记无任何 坑[一二三]/发现[一二三]/我测了/核心能力/真实运行/非演示/副标题 残留。
- 站点重建正常。004/005 源仍未 commit 进 RedBook 本地库（与 006 一起待用户"提交"）。
- **教训**：新规则出台后，不能只应用到最新一篇（006），须回扫既有笔记做一致性补齐；可固化成 skill 的"存量核对 checklist"。

## 小红书独立 HTML 页渲染器（方案 1，已撤销）
- 曾新增 `pipeline/build_xhs.py` 渲染 `content/笔记/*小红书*.md` → `output/小红书/*.html`（base64 内嵌封面）。
- **撤销（用户决策）**：`output/小红书` 位置不对——中文目录混入 gitignored 英文产物区，且"发布稿"被混进"可丢弃产物"。用户选"撤销小红书 HTML"。
  - 已删除 `output/小红书/` 与 `pipeline/build_xhs.py`；技能规范/README 从未写入该流程，无悬空引用。
  - **回到最初纪律**：小红书版 = 仅发布稿（md + 封面 + 配图，全在 `content/` 内），不进实验室站、不生成独立 HTML 产物。
- 004/005/006 源（笔记 md + 封面 + 出图脚本 + 站点脚本）仍**未 commit** 进 RedBook 本地库，待用户"提交"。

## 实验室站配色去 AI 风（用户：主题色过于 AI 风格，内容不要变）
- **诊断**：AI 味来自 `build_lab_site.py` 的 `STYLE` —— 靛紫强调 `#6d5efc`/`#5338e0`、蓝黑侧栏 `#0f1623`、彩虹高饱和 `DOMAIN_COLORS`（#6366f1/#0ea5e9/#8b5cf6…）、浅蓝紫高亮 `#c7d2fe`。
- **新方案＝墨色+砖红「编辑部风」**（呼应封面白底+暖红，但更沉稳）：
  - `--accent:#b5482f`(砖红) / `--accent-d:#8f3522` / `--accent-soft:#f4e9e4`
  - `--side:#23211c`(墨棕，替代蓝黑) / `--side2:#322e27`
  - `--bg:#faf9f6`(暖白纸) / `--ink:#2b2823`(墨字) / `--line:#e8e3d8`(暖灰线)
  - `--green:#4a7a52`(墨绿完成态)
  - `.side` 渐变改墨棕 + 右边框 `rgba(181,72,47,.25)`；`.brand`/cur 高亮 `#c7d2fe`→`#e3b6aa`、indigo 高亮→`rgba(181,72,47,.20)`
  - 标题 `#0f172a`→`#23211c`；tooltip/代码树/灯箱蓝黑→墨棕；`.todo` 冷白→暖白 `#fdf8f5`
  - `DOMAIN_COLORS` 10 色全换大地/莫兰迪系（苔绿·灰青·浅苔·赭黄·砖红·藕荷·橄榄·灰玫·陶橙·中性灰）
- **范围**：仅改 `pipeline/build_lab_site.py` 的 `STYLE` 与 `DOMAIN_COLORS`，重新 build；**未动任何笔记内容与站点结构**。
- 已 `grep` 验证 `style.css` 无 `#6d5efc`/`#0f1623`/`#c7d2fe`/原彩虹色残留。预览 `http://localhost:8766/`。
- 此配色改动属源改动，需与 004/005/006 笔记一起待用户"提交"进库。

## 出图配色去红绿（用户：图集中在红绿配色不好看）
- **诊断**：`fig_quality.py` 语义色 `C_WRONG=#c0392b(红)`/`C_CORRECT=#2c7a4b(绿)` 为纯红绿配对——最易混淆的色盲色对（~8% 男性），且刺眼。004 硬编码同款红绿、005 还有旧暖纸底 `#eee8db`+蓝 `#2b6cb0`+红 `#c53030`；用户截图那种 RdBu 红-白-蓝分歧色条是 matplotlib 默认热力图配色，未来熵/热图会踩。
- **新配色（墨色编辑部风+色盲友好）**：`C_WRONG=#b5482f`(砖红,暖) / `C_CORRECT=#2f7d72`(青瓷绿,冷) / `C_DATA=#2c3e50`(墨蓝) / `C_MUTED=#9aa0a6`(灰)。砖红↔青瓷绿为冷暖对比(色相相隔~160°)，色盲可辨，且与站点强调色一致。
- **新增 colormap**：`CMAP_SEQ=paper_ink`(纸→墨单色顺序，适合熵/信息量) / `CMAP_DIV=teal_brick`(青瓷绿→砖红分歧，零基准在中间)；禁止 matplotlib 默认 `RdBu`。`save_clean` 默认白底（去 `#eee8db`）。
- **落地**：004/005/006 三个出图脚本改为 import `fig_quality` 语义色（004/005 原硬编码已回改为 import）；grep 复核 pipeline 下无 `#c0392b/#2c7a4b/#c53030/#2b6cb0/#eee8db/#888/RdBu` 残留。三图已重生成、站点已重建。
- **技能同步**：`bioSkills-figure-quality`(SKILL.md+fig_quality.py) commit `c097a73..df7613a` 已 push；纪律固化"禁纯红绿、用砖红+青瓷绿、热图用 CMAP_SEQ/CMAP_DIV、白底"。
- 此改动属源改动，需与 004/005/006 笔记 + 站点脚本一起待用户"提交"进库。

## 站点部署：GitHub Pages + Actions（路由 GitHub，已就绪待上线）
- **决策链**：用户确认"网站已生成(output/bioSkills-site)→要部署→选 GitHub Pages+Actions 自动构建"。
- **重构 build_lab_site.py**：第6行 `BASE="/Users/zhangdiandian/RedBook"` 硬编码 → 改为按脚本位置推导(优先环境变量 REDBOOK_BASE，否则 `os.path.dirname(os.path.dirname(os.path.abspath(__file__)))`)，使脚本在 CI 跨环境可跑。本地 anaconda(markdown 3.4.1) 验证构建正常(10 文件)。
- **新增 .github/workflows/pages.yml**：push main → setup-python 3.12 → pip install markdown → clone 上游 GPTomics/bioSkills 到 content/库/bioSkills(gitignored，CI 时取) → 跑构建 → upload-pages-artifact(output/bioSkills-site) → deploy-pages。
- **仓库**：`gh repo create redbook-bioskills-lab --public --source . --remote origin`(Diandian-ops 名下)；commit 仅 2 文件(.github/workflows/pages.yml + pipeline/build_lab_site.py)并 push；`gh auth setup-git` 让 git 用 gh token。
- **Pages 启用**：`gh api -X POST /repos/Diandian-ops/redbook-bioskills-lab/pages -f build_type=workflow` → html_url `https://diandian-ops.github.io/redbook-bioskills-lab/`(public)。
- **卡点（外部）**：Diandian-ops 账号 GitHub Actions 被锁(`account is locked due to a billing issue`)，CI 构建跑不起来，站点暂未实际上线。用户选"先不动，看账号"。
- **待办**：用户去 GitHub Settings>Billing 解锁后，rerun "Deploy to GitHub Pages" workflow 即上线（任务 T7 跟踪）。
- **架构纪律保持**：output/ 仍 gitignore(产物可重建)、content/库/bioSkills 仍 gitignore(外部 clone)，符合"源可重建、产物不进库"。

## 修正：git 作者统一 + 仓库改名（用户纠正）
- **作者统一**：用户指出 commits author 为 `ShengXinF3`(本地 git config user.name/email 遗留)，要求统一为 `Diandian-ops`。
  - 改**仓库级** config：`git config user.name "Diandian-ops"` / `user.email "Diandian-ops@users.noreply.github.com"`(未动全局，避免影响其他项目)。
  - 用 `git filter-branch -f --env-filter` 重写 main 全部 commit 的 author/committer 为 Diandian-ops；先 `git stash -u` 保护未提交改动，重写后 `stash pop` 恢复。
  - `git push --force origin main`(--force-with-lease 因 filter-branch 重写 remote-tracking 报 stale info，改 --force)。远程 commits 现 author=Diandian-ops(noreply 邮箱能正确归属账号头像)。
- **仓库改名**：`gh repo rename bioskills-lab`(原 `redbook-bioskills-lab`)→ `https://github.com/Diandian-ops/bioskills-lab`；`git remote set-url origin` 更新；Pages 自动跟随新 URL `https://diandian-ops.github.io/bioskills-lab/`。
- **教训**：本仓库应始终以 Diandian-ops 身份提交；若发现 author 异常，用 filter-branch 重写 + --force 推送(仓库无协作者，安全)。
- **账号解锁 + 站点实际上线（16:32）**：
  - 用户重新绑定支付方式后，GitHub Actions 账单锁解除；rerun 暴露真实构建错误：`FileNotFoundError content/笔记/006-bioSkills真实试用-alignment-trimming.md`(CI 干净 clone 缺 006 源)。
  - 根因：部署时只 commit 了 workflow+脚本，006 专题源(笔记/素材/pipeline)留在本地未进库。`.gitignore` 并未忽略 `content/笔记/`，笔记本就是应进库的源。
  - 修复：`git add` 006 两个笔记 + `content/素材/006-trimming/` + `pipeline/006-trimming/` 并 push；rerun 后 build+deploy 全绿。
  - **站点已实际上线**：`https://diandian-ops.github.io/bioskills-lab/`(首页/配图/006 页均 HTTP 200)。006 页面真实路径为 `alignment/alignment-trimming.html`(在子目录下)。
  - 教训：`content/笔记/` 与 `content/素材/` 下**新增专题必须随部署一并 commit**，否则 CI 缺源构建失败。之后每写新专题，记得把该专题的笔记+素材+pipeline 提交进库，push 即自动重建发布。
- **内容源全量同步到线上（16:42）**：
  - 用户确认把 001-005 笔记/素材/pipeline + README 与本地一致；**排除 `.workbuddy/` 记忆文件**(agent 内部日志，不进 public 仓库)。
  - `git add content/ README.md pipeline/`(dry-run 17 文件)→ commit `efa1d96` → push main → **push 自动触发** Pages 重建(无需手动 rerun)→ build+deploy 全绿。
  - 同步删除了本地已删的 `003-…_dingcard.md`(线上一致，git 历史可恢复)。
  - 验证：首页 / `/alignment/` / 三专题页均 HTTP 200。线上内容与本地完全一致。
  - 遗留技术债：`pages.yml` 用的 `actions/checkout@v4` 等仍 target Node.js 20(GitHub 已 deprecated，强制跑 Node 24)，功能不受影响但有弃用警告，后续可升级 actions 版本消除。
- **新增小红书 3:4 出图脚本（17:16 样例验证）**：
  - 用户需求：把站点渲染效果转成图片发小红书。明确方向：**站内深拆版 md → 竖版 3:4 多卡图**。
  - 环境：新建 managed python venv（`~/.workbuddy/binaries/python/envs/default`）隔离安装 `markdown`、`playwright`、`Pillow`；`playwright install chromium` 下载约 95 MiB。
  - 脚本：`pipeline/md_to_xhs_images.py`：读 md → 去 META → 转 HTML 套竖版模板（白底+暖红 #b5341f+中文字体）→ Playwright 全页截图 → 按 1080×1440(@2x=2160×2880) 切成多卡 → 输出到 `output/xhs-images/`。
  - 006 样例(`006-bioSkills真实试用-alignment-trimming.md`)产出 2 张 2160×2880 PNG，中文/表格/代码块/配图渲染正常，已展示给用户。
  - MVP 局限：固定高度切片会导致第二张卡从段落中部开始，未按 H2 节边界对齐；后续若需优化，可改为按 H2/H3 节边界智能分页。
  - 用法：`.workbuddy/binaries/python/envs/default/bin/python pipeline/md_to_xhs_images.py content/笔记/00X-bioSkills真实试用-*.md`。004/005 可用同命令批量跑。
- **评估并接入 md2card 库做小红书出图（17:56 起）**：
  - 库位置：`content/库/md2card/`，第三方独立 git 仓库（origin=haodongcui/md2card，MIT 协议），**未进 bioskills-lab git**（git status 显示整个目录 `??` 未跟踪；只有 `content/库/bioSkills/*` 被 gitignore）。
  - 它是什么：纯前端 Markdown→小红书卡片工具（Preact+Vite+TS），核心是**真实 DOM 测量分页**（H2/H3 标题簇保护、页尾安全区、宽表/长代码按行续页、`<!-- md2card:break -->` 手动分页），输出 3:4 标准 1080×1440 / 高清 2160×2880，与我们脚本尺寸完全一致。
  - 约束：**无 CLI、无服务端导出**，导出依赖 html-to-image（真实 DOM+嵌字体），只能浏览器跑；不能直接 `node` 调它出图。
  - 融合决策（用户拍板）：走 **B 本地可视化工作台** 路线——在 md2card 跑 dev server，浏览器导入 md 手动微调主题/封面后导出 PNG ZIP；而非 A（改写脚本做 headless 智能分页）。理由：工作台效果最可控，用户要的是可视化微调。
  - 环境已搭好：managed Node 22.22.2 `npm install`（135 包）→ `npm run dev`（vite --host 0.0.0.0，端口 5173）已后台常驻。验证 `http://localhost:5173/` 与 `/app/` 均 HTTP 200，标题「Md2Card · Markdown 转小红书图片」。
  - 后台任务 id：`NBktjB`（dev server）。用户浏览器访问 `http://localhost:5173/app/` 即可用。
  - 操作要点（已告用户）：①导入 `content/笔记/00X-*.md`；②因浏览器无父目录权限，导入后需在"管理图片→自动补齐"选对应 `content/素材/00X-*` 文件夹绑定本地图；③右侧调主题(minimal/editorial/notebook/research)/封面策略/密度/画布 3:4；④点"下载图片"选标准发布或高清原图，得 PNG ZIP 直接发小红书。
  - 关键纪律：`content/库/md2card` 是第三方 repo，**绝不 commit 进 bioskills-lab**；保持 untracked 本地参考。若要保留可追溯，可后续加为 git submodule 指向 haodongcui/md2card（pinned）。
  - 20:20 用户进一步要求：**把手动点击变成预设选项自动产出**。于是改走"Playwright 驱动 md2card 工作台"路线，相当于在保留 md2card 真实 DOM 分页优势的前提下做了自动化包装。
- **md2card 自动出图脚本完成并验证（20:23）**：
  - 脚本：`pipeline/md2card_automation.py`，用法：`.workbuddy/binaries/python/envs/default/bin/python pipeline/md2card_automation.py content/笔记/00X-*.md --theme research --density balanced --cover integrated --canvas 3:4 [--hd]`。
  - 自动化链路：Playwright headless 打开 `http://localhost:5173/app/` → 删除 `window.showDirectoryPicker`（强制走隐藏 input 回退）→ 用"导入 Markdown"file input 灌入 md（触发 `loadFile` → 自动弹图片绑定对话框）→ 第 2 个隐藏 input(`markdownAssetFolderInputRef`，webkitdirectory) 传素材目录完成绑图（后缀匹配命中引用）→ 切换设置分类逐项点选主题/密度/封面/画布 3:4 → 点"下载图片"→ 等 preflight ready → 截获浏览器下载 ZIP → 解压到 `output/xhs-images-md2card/<slug>/`。
  - 006 样例验证：`006-bioSkills真实试用-alignment-trimming.md` → 自动产出 **10 张 1080×1440 PNG**（01.png~10.png），封面、代码块、表格、中文均正常，已展示给用户。
  - 关键依赖：md2card dev server 必须已在后台跑（端口 5173，任务 id `NBktjB`）；venv 已装好 playwright/Pillow/markdown。
  - 纪律重申：`content/库/md2card` 仍是第三方 untracked 参考库，不 commit 进 bioskills-lab；自动脚本本身是自己的文件，应进库。
- **md2card 高清出图已验证（20:38）**：用户问"是不是高清图"，先核验发现默认产出是 1080×1440 标准发布；随即用 `--hd` 参数重出 → 006 专题 **10 张 2160×2880 高清原图 PNG**，已通过 PIL 核验尺寸，已展示给用户。
- **md2card standalone 独立封面版出图（21:06）**：用户确认"重跑"独立封面 → 用 `--cover standalone --out output/xhs-images-md2card-standalone` 重出 006 专题 → **11 张 2160×2880 高清 PNG**（比 integrated 多 1 张封面卡），01.png 为纯独立封面，正文从 02.png 开始。原 `output/xhs-images-md2card/...` integrated 高清版保留未动，两版目录并列便于用户对比挑选。
- **md2card 默认主题改为雾松笔记 notebook（21:11）**：用户问默认主题+能否改"雾凇笔记"。查 md2card 源码确认内置四主题 `{minimal:纯净排版, research:雾蓝实验室, editorial:柔光浅紫, notebook:雾松笔记(绿色·清透)}`，"雾凇"即 `notebook`（源码用"松"非"凇"）。将 `pipeline/md2card_automation.py` 的 `--theme` 默认值由 `research` 改为 `notebook`，并出 006 的 notebook+standalone+hd 版到 `output/xhs-images-md2card-notebook/`（11 张 2160×2880），与 research 蓝调版并列对比。
- **005 雾松笔记独立封面高清版出图 + 修复素材探测（21:12）**：用户要求 005 也用默认参数（notebook+standalone+hd）。首次跑发现 `find_assets_dir` 未探测到 `content/素材/005-msa-statistics`（返回 None），导致配图未绑定。原因=原末段拆分逻辑在某些命名下会漏正确候选。已用 Python 直接替换函数：改为基于 `content/素材/` 目录扫描所有 `00X-*` 前缀目录作为兜底候选。修复后验证 `005->content/素材/005-msa-statistics`、`006->content/素材/006-trimming` 均正确。重跑 005 成功绑定素材，产出 **10 张 2160×2880 高清 PNG**（01.png 独立封面，08.png 含 005-fig.png 真实数据图），落 `output/xhs-images-md2card-notebook/005-bioSkills真实试用-msa-statistics/`。
- **新增「小红书正文描述」文件形态（21:18）**：用户指出 005 小红书帖「正文应该有信息」，发现现有 `005-bioSkills小红书-*.md` 只是 md2card 出图源稿（卡片内容），没有独立「帖子正文文本框」文案。于是新建 `content/笔记/005-bioSkills小红书-msa-statistics-正文.md` 作发表稿正文（与出图源稿分离，不进站点、不参与 md2card）。META 内附「标题建议」。
  - 迭代：首版~450字→用户嫌多；压到~160字→用户嫌「跳跃看不懂」；加过渡后~230字用户嫌「僵硬、像教科书讲道理、还是偏长」；再改笔记口吻(去"不能套/别用"说教、保留4.32对比)→用户嫌「给读者看但开头突兀」；最终加 2 句读者向铺垫(MSA后常算信息量+skill干啥)自然过渡到"我跑了一遍踩到一个点"。定稿特征=短、顺、读者向铺垫、像自己记笔记、不居高临下。
  - **用户验收结论**：确认此风格=「介绍背景 → 做了什么 → 得到结论 → 给出判断」四段，读者向、不突兀、短而顺。
  - **004/006 同款正文补齐**：已按定稿风格新建 `004-bioSkills小红书-pairwise-alignment-正文.md`（两点：默认gap=0塞46假gap / PID四口径差2.8pct）与 `006-bioSkills小红书-alignment-trimming-正文.md`（kpic-smart-gap只砍1列/保留率95.2% + --log审计）。现 004/005/006 三篇均有独立正文描述文件。
  - **005 正文对齐重写**：用户指出 005 的「做了什么/结论」也需重写以对齐 004/006。改动：①「做了什么」补具体脚本名 `msa-statistics 的 entropy_analysis.py`（与 004 点 PairwiseAligner、006 点 ClipKIT kpic-smart-gap 一致）；②「结论/判断」补明确「所以…」收束句（原版只到数字即断）。三篇现结构完全统一（背景→做了什么含工具名→结论数字→判断）。
  - **005 再次精简（21:31）**：用户评上一版「不自然、过于细节、文字也多」。已砍：频率百分比(9.2%/1.3%)、4.32魔法数、脚本名、18列/11.4%/2.778 数字串；解释合并成一句顺句；保留核心对比(Leu3.44/Trp6.23)+判断句。现约140字。→ **用户偏好落定**：小红书正文要"短、自然、像笔记、不堆数字细节"，与 004/006 的"数字多但自然"略有差异，005 按用户最新指令以最精简为准。
  - **skill 固化**：在 `redbook-bio-note-writer/SKILL.md` 新增「九、小红书帖子正文描述独立文案」节，规定 -正文.md 分离文件、四段结构(背景/做了什么/结论/判断)、读者向短顺风格纪律、命名示例。后续新主题直接套用。
- **005 正文多轮返工 + 独立规范 skill 建立（21:38）**：用户连番挑出 005 正文语病/逻辑问题，逐条修：
  - 去"我拿8条globin原样跑了一遍"的证明感 → "拿示例数据试了下"。
  - 裸数字 `Leu→3.44/Trp→6.23` 读者不懂 → 加 `常见/稀有` 标签 + 说明 bits=信息量 + 对照 uniform 被拉平。
  - "同样保守，稀有的反而信息量更高" 的「反而」逻辑错（稀有更高是符合直觉非反转）→ 删「反而」。
  - "套…假设"生硬 → "套用"；解释句补前提"在uniform下"。
  - **关键冗余 bug**：原第12行"uniform反而算丢"与第17行"uniform下俩会被算成一样"重复说同一观点，读着难受 → 改为 uniform 问题只在第一段说一次，Robinson 段只亮数字"差异这才显出来"，删重复。
  - **定稿 005 正文**：背景铺垫→试了下踩点→uniform抹平稀有信号(一次说清)→Robinson Leu3.44/Trp6.23显出差异→判断用Robinson。逻辑闭环无冗余无反而。
  - **建立独立规范 skill `redbook-xhs-body`**（用户原话"你能否建立正文规范的skills 我真的担心你乱来"）：路径 `~/.workbuddy/skills/redbook-xhs-body/SKILL.md`。内容=定位(与出图源稿分离) + 固定四段结构 + 风格纪律 + **雷区清单8条(每条都真踩过，含禁止证明感/裸数字/误用反而/套用/重复同观点等)** + **自审Gate 8 项 checklist** + 005 定稿作范例。并在 `redbook-bio-note-writer` 第九节顶部加指针指向该 skill，避免双源。目的=以后写 `*-正文.md` 必须先过此 skill 自审 Gate，杜绝反复返工。
- **笔记层面可读性纪律 + 三篇真实试用笔记审计（21:44）**：用户追问"笔记层面的文字是不是也会存在问题 语句要自然通顺"。诊断=管笔记的 `redbook-bio-note-writer` 第四节只有**立场**纪律(客观/不拟人/不居高临下)，**缺"可读性"硬门槛与自审 Gate**，导致"客观但生硬/有语病/甚至标错"能溜过。
  - **skill 更新**：`redbook-bio-note-writer/SKILL.md` 新增「十、可读性纪律与自审 Gate(适用所有笔记形态)」——10.1 可读性纪律(术语首现必解释/禁生硬搭配如"套…假设"→"套用"/长句拆分无语病/小标题客观禁说教词/禁口语隐喻如踩坑/不堆数字也不跳跃/相邻句不重复同观点) + 10.2 自审 Gate 8 项 checklist + 10.3 与 `redbook-xhs-body` 关系(通用可读性覆盖所有形态，写-正文.md 两者都要过)。
  - **005 真实试用笔记实锤修复**（最严重，含内容错）：
    - 第91行小标题「蛋白 IC 不能套 DNA 均匀背景公式」("不能"=说教词、"套…公式"=生硬搭配) → 「蛋白 IC 的背景频率：均匀假设会抹平稀有残基信号」。
    - **表格"差距"列标签写反(内容错误)**：原 Leu 标"低估了稀有性"、Trp 标"高估了常见性" 全错。修正——Leu(常见) uniform4.32>Robinson3.44=**uniform 高估了常见残基**；Trp(稀有) uniform4.32<Robinson6.23=**uniform 低估了稀有残基**；表头"差距"→"uniform 的偏差"；Ala"接近但仍有偏"保留。
    - 第105行「踩这个坑」→「若沿用 dict 式访问」；第169行「陷阱」→「背景频率问题」；第187行「这个坑」→「这个背景频率偏差」；第40行 SKILL.md 行「两大坑」→「两大注意点」；第157行图注「核心坑可视化」→「核心差异可视化」。
  - **004 真实试用笔记修复**：第77行「实战踩坑后」→「实战经验沉淀」；第147行「### 默认 gap=0 陷阱」→「### 默认 gap=0 的配置偏差」；第185行「把"gap=0 是个坑"写成了 warning…撞墙后发现」→「把"默认 gap=0 是错误配置"明确写成了 warning…自行发现」；第195行「实战避坑」「核心坑点」→「实战经验」「核心问题」。
  - **006 真实试用笔记**：通读干净、客观、无口语隐喻，过 Gate，无需改。
  - 审计后 `grep 坑|陷阱|套.*公式` 三篇真实试用笔记已零残留；grep 仅剩命中在小红书引流版/正文(如 005小红书.md:25「核心坑…不能套 DNA 均匀公式」、004小红书.md:19「参数坑很多」、005正文META标题建议「别套DNA公式」)——本次审计范围=真实试用笔记，小红书版/正文留作下一步可选(其风格更 casual，但同样含"不能套公式/坑"等违规，可按同 Gate 顺手清)。
  - 未 commit：本次改动的真实试用笔记 + skill 文件均待用户"提交"进库(连同此前未提交的 004/005/006 源 + 站点脚本 + 配色改动)。

## 笔记形态精简：只留「真实试用 + 小红书正文」两份（21:48 后）
  - **用户决策**：笔记层只保留两类文件——`00X-bioSkills真实试用-<topic>.md`（站内深拆，进站点）+ `00X-bioSkills小红书-<topic>-正文.md`（帖子正文文案）；**小红书引流版 `00X-bioSkills小红书-<topic>.md` 废弃、不再生成、现有文件删除**。
  - **原因**：引流版原是 md2card 出图源稿，现用户判定不需要；output 下已出 PNG 仍保留。
  - **skill 更新** `redbook-bio-note-writer`：第八节两份文件定义去掉引流版、改指正文；8.2 标注「已废弃」并保留 figure-quality 引用；第九节去掉「出图源稿」表述；第八节新增"引流版已废弃"提醒块。
  - **删除执行**：`004/005/006-bioSkills小红书-{pairwise-alignment,msa-statistics,alignment-trimming}.md` 共 3 个移入 macOS 废纸篓（可恢复，非硬删）。验证后 `content/笔记/` 仅剩真实试用×3 + 正文×3 + 早期 001/002/003。
  - **副作用**：`pipeline/md2card_automation.py` 出图源稿缺失，以后若重出图需另定源（从真实试用/正文派生），或脚本改食正文/真实试用。
  - 001/002/003 早期笔记不在本次范围，保留未动。

## md2card 出图路径规范（21:52 后）
  - **用户决策**：保留 md2card 出图能力（源稿=真实试用版 md，删引流版不影响）；输出路径要规范起来。
  - **脚本改动** `pipeline/md2card_automation.py`：① `--out` 默认 `output/xhs-images-md2card` → `output/xhs-cards`；② 产物结构 `<out>/<slug>/<theme>-<cover>[-hd]/01.png…`（variant 子目录，slug=源 md 文件名去扩展名）；③ 导出后清理 .zip 残留只留 PNG；④ docstring/usage/参数 help 同步更新（源稿建议传真实试用版）。managed python py_compile 通过。
  - **迁移**：原散落目录 `output/xhs-images/`(旧 md_to_xhs_images.py 废弃产物)、`xhs-images-md2card/`、`-standalone/`、`-notebook/` 共 42 张 PNG 迁移到 `output/xhs-cards/<slug>/<variant>/`，variant 按推断命名：`research-integrated`(006 早期 research integrated 10张)、`research-standalone`(006 standalone 11张)、`notebook-standalone-hd`(005+006 notebook+hd 共21张)。迁移后删除 4 个旧目录；另清掉遗留空目录 `output/xhs-md2card/`。
  - **skill 更新** `redbook-bio-note-writer`：第八节废弃提醒块追加「出图产物路径规范」一句，指向 output/xhs-cards 结构与脚本。
  - **注意**：历史产物 variant 命名系推断（旧目录名未存 cover/hd 参数），若某批实际参数不符可告知重命名。
  - **全工作流自动化可行性评估（用户问"能否全流程自动"）**：文字规范 skill（redbook-bio-note-writer 第十节 + redbook-xhs-body）即"纪律层+自审层"控制实体，但它们只控格式/质量门槛、不控内容生成。逐阶段：S1 选题(不可,人判断)、S2a 真跑(可脚本跑文档命令抓输出)、S2b/S3 笔记与正文(半自动:模板脚手架+格式 lint 可自动,内容需人写)、S4 出图(已全自动化)、S5 发布(不可,无 XHS API)。结论=全流程零人值守不可行也不该(内容级错误如 005 表方向写反需人核验)；现实目标 human-in-the-loop：AI 跑 S2a+出图+套 Gate lint，人负责选题/写内容/发帖。可下一步建 S2a→S4 编排脚本 + Gate lint 器(自动抓 坑/陷阱/不能套/反而/裸数字/相邻重复 等可 lint 违规)。

## 自动化工具落地：gate_lint.py + trial2xhs.py（22:25 后，用户"需要的"批准）
  - **新建 `pipeline/gate_lint.py`**（机检器）：把 redbook-bio-note-writer 第十节自审 Gate + redbook-xhs-body 雷区里**机械化可检**条目转成规则。模式自动判别：`*-正文.md`/含`-小红书-` → 小红书正文严格模式(额外查 证明感/裸数字)；其余 → 笔记可读模式。规则：ERROR 级(拦下)= 口语隐喻(踩坑/陷阱/撞墙等)、生硬搭配(套…假设/公式→套用)、小标题说教词(不能/别/千万别)、正文证明感(原样跑了一遍)、；WARN 级(人眼复核)= 反而、裸数字(无标签/单位/对照)、未释缩写(IC/PID/MSA/.. 首现无注)、相邻句重复(重叠>0.5)。META 块(<!-- META…/META -->)内行豁免发布正文规则。退出码：有 ERROR→1，否则→0。`python pipeline/gate_lint.py <file.md> [--json|--quiet]`。
  - **校验结果**：6 份现有文件跑 lint——3 篇正文(004/005/006)均 0 ERROR 0 WARN；3 篇真实试用均 0 ERROR(仅 1-2 WARN 可接受)。期间发现并修了 004/006 正文残留"原样跑了一遍"证明感(对齐 005 定稿)；005 标题建议"蛋白IC别套DNA公式"因在 META 内且系用户钦定钩子，lint 豁免不修。
  - **新建 `pipeline/trial2xhs.py`**（编排器）：入口 `python pipeline/trial2xhs.py <真实试用笔记.md> [--force|--no-server-check]`。流程：① Gate1 跑 gate_lint，ERROR 且非 --force → 退出(不出图)；② Gate2 探活 md2card dev server(localhost:5173)，不通→给启动指引退出；③ 调 md2card_automation.py 出图，默认 notebook/standalone/hd，产物落 output/xhs-cards/<slug>/notebook-standalone-hd/。设计=只自动化"格式门槛+出图"，不自动化选题/写内容/发帖(human-in-the-loop)。
  - **全链路验证(真实跑通)**：md2card dev server 仍在跑(5173 UP)。依次跑 004/005/006 真实试用笔记 → 全部 Gate 过 + 出图成功：004→11张、005→10张、006→11张 notebook-standalone-hd PNG，落 output/xhs-cards/<slug>/notebook-standalone-hd/。证明自动化段(S2a-lint→S4)闭环可用。
  - **skill 更新** `redbook-bio-note-writer`：第八节"出图产物路径规范"块追加自动化入口说明，指向 trial2xhs.py(编排器)与 gate_lint.py(机检器)，要求写新篇前先自测 gate_lint。
  - **待 commit**：新增 pipeline/gate_lint.py、pipeline/trial2xhs.py；修改 004/006 正文(去证明感)；修改 redbook-bio-note-writer/SKILL.md；新出 004/006 notebook-standalone-hd 图。连同此前未提交的笔记审计/skill/出图脚本均待用户"提交"进库。

## 提交完成 + git 中文字路径排查坑（16:42 收尾）
- **已提交** `32f3cec`(main)：gate_lint.py + trial2xhs.py 新建、md2card_automation.py 改写、004/005/006 真实试用改写(客观描述)+ 删除引流版 + 新增 正文.md、006-xhs-cards 仅留 notebook-standalone-hd、memory 日志。14 files / +831 -183。素材 PNG 早已在 HEAD(efa1d96 那次 commit 已带进库)，本次 diff 不含它们属正常。
- **未提交(有意排除)**：`content/库/`(外部 clone, gitignore 子目录)、`pipeline/md_to_xhs_images.py`(废弃旧脚本)。
- **排查坑(重要，省下次时间)**：macOS 上 `git status/ls-files/ls-tree` 默认对含中文路径做**八进制转义引用**(`素材`→`\347\264\240\346\235\220`)，导致 `grep -F "素材"`/`grep "素材"` 永远 0 命中 = 假阴性。正确核查方式：
  - 判文件是否在 HEAD/index：用 `git rev-parse HEAD:<path>`(返回 40 位 hash 即在)或 `git ls-files --error-unmatch <path>`，别用 grep 文本。
  - 要看真实字节路径：`git ls-files -z`(NUL 分隔、不转义) 再 decode；或 python `subprocess` 读 `git ls-files -z`。
- **目录别混**：笔记在 `content/笔记/`(E7 AC 94 E8 AE B0)，素材在 `content/素材/`(E7 B4 A0 E6 9D 90)，两者字节不同。`git add content/素材/` 只动素材、不会碰笔记；本次笔记改动要单独 `git add content/笔记/`。
- **未 push**：用户只说"可以提交"，未要求 push；main 已领先 origin/main，待用户确认再 push。
- **早前 `git update-index --cacheinfo` 误写路径串**导致的 `\347\264\240…` 字面反斜杠损坏条目已随 soft-reset + reset HEAD -- . 清除，现 HEAD tree 损坏条目数=0。

