# 2026-09-02 工作日志

## 发布台 AI 改写风格改版（承 09-01，已生效）
- 用户明确要求：AI 改写结果要「简单明了、通俗易懂、不需要 markdown 语法、小红书图文风格、文本格式即可」。
- 改动落在 `output/xhs-site/scripts/build_xhs_console.py`（生成器=唯一真相源），共 5 处提示词：
  `AI_STYLES.neutral / condense / demarket`、`AI_STYLES_OBJECTIVE`、自定义要求默认 note。
  全部由「直接输出 markdown（保留格式与分段）」改为「直接输出纯文本，禁用 #/**/-/> 与代码块，用自然空行分段，符合小红书图文正文习惯」。
- 重跑生成器重建 `index.html`；`serve_console.py` 是 `SimpleHTTPRequestHandler(directory=ROOT)`，**每次 GET 从磁盘读**，
  故改 UI 只需重跑生成器 + 浏览器硬刷新，无需 kill/重启 8899（此前记忆里「必须重启 8899」的说法对本场景不准确）。
- 验证：curl 8899 返回页含新提示 4 处、旧 markdown 提示 0 处。

## 新模块选定：alignment-files（用户拍板）+ WSL 决策
- 用户从 4 个候选里选了 **alignment-files**（10 个 skill：sam-bam-basics / alignment-sorting / indexing /
  filtering / validation / bam-statistics / pileup-generation / duplicate-handling / reference-operations /
  amplicon-clipping）。编号从 **028** 起（001–027 已用）。
- **关键环境事实（本次实测，覆盖 09-01 旧结论）**：
  - samtools 不在 PATH、全盘无安装、无 msys2/pacman、无 WSL/Docker/conda；`pip install pysam` 因 Windows
    无预编译 wheel 且缺 htslib/编译器而构建失败。→ **alignment-files 现状是「数据有、工具无」**，
    与 022/023/027 的「输入数据结构性缺失」性质不同。
  - **但真实 BAM 输入不缺**：content/素材/read-alignment/011-bowtie2-alignment（9 个 BAM/SAM）、
    012-bwa-alignment（7 个）、013-hisat2-alignment（4 个），均带 .bai 索引，8/30 20:20 生成。
  - 系统：Windows 11 家庭版 Build 26200；WSL 功能 **Disabled**、VirtualMachinePlatform **Enabled**、
    HypervisorPlatform Disabled；PowerShell 当前即管理员。
- **用户决策：装 WSL（而非走难度说明路线）**。理由：562 个 skill 里大半是 Linux 工具，WSL 是最高杠杆的一次投入，
  可解锁后续绝大多数模块。
- WSL 装好后可直接读 `/mnt/d/1.WorkDir/RedBook/...`，现有 20 个真实 BAM 无需重新下载即可当输入。

## WSL + samtools 已就绪（用户 19:20 确认装完，实测可用）
- 用户执行 `wsl --install -d Ubuntu` 后，**无需重启**：Ubuntu 已 Running(VERSION 2)，旧「WSL 功能 Disabled」只是遗留标志。
- 调用方式（Bash 工具里）：`wsl.exe -d Ubuntu -u root -- bash -lc "<命令>"`（root 免密，可 apt）。
- 已 `apt-get install -y samtools` → **samtools 1.22.1 / htslib 1.22.1**。
- 端到端验证：`samtools flagstat /mnt/d/1.WorkDir/RedBook/content/素材/read-alignment/011-bowtie2-alignment/aligned_e2e.bam` → 6000 reads；`samtools view -H` → 真实 header。证明 WSL samtools 可直接读 Windows 盘 BAM。
- **结论：alignment-files 等所有依赖 samtools/htslib 的模块现在可真跑**，不再走「难度说明」路线。本机 Windows 侧仍无 samtools/msys2/conda，全部走 WSL。
- 注意：Bash 工具禁止直接调 `cmd.exe`/`powershell.exe`；查 WSL 用 `wsl.exe`，查 Windows 功能用 PowerShell 工具（且 PowerShell 多位置参数要逐个传，别用数组）。

## 项目盘点（决定下一步）
- 三大类 **24/24 全部收口**：alignment 004–010(7)、read-alignment 011–014(4)、variant-calling 015–027(13)。
  每篇均具「深拆版 + 小红书精简版」双文件、四主题卡片、保真度核查、gate_lint 全绿。
- 上游 bioSkills 共 **562 个 skill**，已覆盖仅 24，**未覆盖 538**——继续即开新模块。
- 规模参考：workflows 41、chemoinformatics 20、data-visualization 20、single-cell 17、crispr-screens 15、
  database-access 15、comparative-genomics 13、atac-seq/chip-seq/clinical-biostatistics/clinical-databases/clip-seq/spatial-transcriptomics 各 12、
  causal-genomics 11、copy-number 11、alignment-files 10。
- 已提交推送（09-01）：c649889 站点更新时间 → 654c411 发布台路径重构 → 5026d03 工作记忆 → 0dd1877 清 linsi_stderr.log 噪声。
  工作树已 clean。
