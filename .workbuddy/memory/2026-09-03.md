# 2026-09-03 工作日志

## 001–014 bioSkills 真实试用 — 全部收尾完成
- 001–003 按约定排除（早期随笔，无单一 SKILL.md）；004–014 全部真实试用。
- 004/005/008/009 = 纯 Python（commit 0ee59b6，已推）；006/007 = 916ed39；010 = 485b1e5；011–014 = 207f470（HEAD，已推 origin/main）。
- 全部 11 篇（004–014）gate_lint ERROR=0 WARN=0，出图自检 TOTAL FAILS=0，素材目录自带数据可独立复现。
- 重型 CLI 7 个（006 trimming / 007 multiple / 010 structural / 011 bowtie2 / 012 bwa / 013 hisat2 / 014 STAR）均 WSL bioconda `bio` 环境真跑；014 STAR 因 Agent B 旧产物是沙箱空 BAM，gp-9 已诚实重跑（99.87% uniquely mapped）。

## 四主题卡片 + 发布台同步（本日完成）
- 起 md2card dev server：5173 已被早期会话实例占用（PID 6544，确为 Md2Card，/app/ 返回 200），本会话新起的移到 5174 已 kill，单实例规则保持。
- 跑 `.workbuddy/gen_cards_006_014.py`（7 skills × 4 themes 串行）：SUMMARY gen=28 ok=28 skip=0 fail=0 missing=0，每 skill 12 PNG（4 主题 × 3 页）。
- 卡片为纯文本（7 个 card-ms 摘录稿均 0 图引用，按设计不嵌图），故 006 资产目录名 `006-trimming` 与 slug `006-alignment-trimming` 不一致**不影响卡片**（仅 build 警告，无害）；站点图仍解析正常（笔记引用 `006-trimming` 与目录自洽）。
- `build_xhs_console.py` 重跑：index.html 799KB，006/007/010/011/012/013/014 全部 `imgs=3 title=有 body=有 md=有`，发布台已同步。

## 清理（用户已确认"全部确认清理" → 已执行）
- 已删：错误分类副本 `content/素材/alignment/011–014-*`（20M/6.7M/8.2M/空）+ `content/笔记/alignment/011-012-*`（4 个笔记）+ root 临时文件 `_fetch_data.py`、`tmalign__.txt` + 一个 `U+F022` 私有区字符名的 0 字节垃圾文件。
- 保留：`.workbuddy/gen_cards_006_014.py`、`gen_cards_029_037.py`；`wsl_*` 环境脚本。
- 复核：权威版 read-alignment/ 仍在库内（660 资产 + 8 笔记），无数据损失；`git status -s` 干净只剩保留项。
- **踩坑**：删 011/012 笔记时把 `-正文` 后缀搞反了——实际是「小红书版**带** `-正文`、真实试用版**不带**」。按反向名字 `rm -f` 静默无匹配，文件残留；须 `ls | grep` 复核确认真删掉再报完成。

## 验收（最终态，2026-09-03）
- `git status -sb` = `## main...origin/main`；`git rev-list --left-right --count origin/main...HEAD` = **0 0**（全量已推，无落后/领先）。
- 已提交笔记：真实试用 11 篇 + 小红书 11 篇（004–014）；素材 alignment 116 + read-alignment 660。
- 卡片：本轮 7 skills × 4 主题 × 3 页 = 84 PNG；xhs-cards 全库累计 1179 PNG。
- **坑（新增，影响验收）**：`git ls-files` 对中文路径默认 octal 转义（`core.quotepath`），直接 `grep -c 真实试用` 恒为 **0 假阴性**（一度误判"笔记没入库"）。验收中文路径必须用 `git -c core.quotepath=false ls-files ...`。与既有「Bash `cat` 读中文路径显示为空」同类。

## 备注
- 006 资产目录命名不一致（`006-trimming` vs 其余 `00X-<fullslug>`）：功能无损（卡片纯文本、站点图引用自洽），若需统一可 git mv + 改笔记 3 处图引用 + 重建站点，待用户定。
- 未跟踪保留项（正常，未提交）：`.workbuddy/memory/2026-09-02.md`(M)、`2026-09-03.md`、`gen_cards_*.py`、`wsl_*.sh`。
- 10 个 general-purpose 子 agent（gp-1…gp-10）全部完成/闲置，其产出已随 0ee59b6…207f470 入库。

## 022/023/027 补真跑收尾（2026-09-03 晚，"所有问题优化好再进下一模块"批次）
- **022 gatk**：bioconda 建 `bio-gatk` env 装 **GATK 4.6.2.0**（GitHub 不可达不再是阻塞，2026-09-01 评估作废）。HaplotypeCaller 标准模式 0 变异 = **正确行为**：reads 由 `wgsim -e 0.02 -r 0` 生成、零真突变；四重证据：修正统计（排除 `.` 与 `,`）非参考列 4659/11962（原 11152 是 awk bug 作废）、bcftools call 同为 0、最高 ALT 支持 5 reads/18.5%、error rate 2.19%≈0.02。GVCF 2946 条 `<NON_REF>` 区块。双文件+素材（_diag/make_figs/repro/3 PNG）+ gate_lint 0/0 + TOTAL FAILS=0。commit **13c7c2a**。
- **023 joint-calling**：bcftools 1.24 WSL 真跑。naive merge 636 records 缺 **1924** 基因型（全回填）vs joint **0** 缺失；GT 不一致 0–3/636；总基因型 naive 3816 vs joint 32586。双文件+素材+自检过。commit **65a3748**。
- **027 deepvariant**：复核更新（未真跑维持），阻塞点收敛为「工具本体不可获取」（无容器运行时/PyPI 无包/GitHub 不可达），BAM+参考已具备的更正已写入笔记。commit **da76b41**（已推）。
- **卡片张数超标再验证**：真跑深拆稿直接出图 022=21 张、027=19 张（>18 平台上限）→ 摘录稿方案（`output/xhs-site/card-ms/variant-calling/<slug>.md`，slug=卡片目录名）压到各 3 页/主题（12 张/组）；023 深拆直出 16 张/主题合规。发布台已同步（022=3、023=16、027=3）。**真跑深拆稿普遍变长，出图默认走摘录稿**。
- **待办**：`65a3748` + `13c7c2a` 因 GitHub TLS 握手失败暂未推（schannel handshake，直连与 7897 代理均 000，间歇性故障），网络恢复后 `git push origin main`。
- **001–003 决定**：维持归档（早期随笔、无单一 SKILL.md，硬套试用模板=无中生有），不改造、不进站点/发布台。

## read-qc 收尾进度（20:40 更新）
- 038/042 独立复核通过（gate_lint 双 0、check_figs FAILS=0）→ 已提交并推送（与 044 同批，287d763 前）。038 实测复现 FastQC Encoding +64 误判；042 实测 directional 分子计数 +0.22% 最准、N-UMI string 法放行。
- 根目录发现 0 字节垃圾文件（文件名=U+F022 转义污染，agent 重定向引号被吞产物），已删除并披露。
- gp-039/gp-043 会话中断后经 SendMessage 恢复：039 缺 repro_transcript+双笔记，043 仅 transcript；043 已走真实迷你库路线（refs/phix.fna 落盘）。
- 待办：039/043 到货→验→提交；build_lab_site.py TRIALS/READQC_SUBS 与发布台 CATS 的 read-qc 注册改动尚未提交（等站点重建一起走）。
- gp-039/gp-043 SendMessage 恢复均撞 429（默认模型配额 22:11 重置）→ 按 022 批次先例换 lite 模型重派：gp-039b（只补 transcript+双笔记，素材已在盘）与 gp-043b（从 repro_transcript 断点续做真实迷你库路线）。
- 039 补件到货→独立复核通过（gate_lint 双 0、check_figs FAILS=0）→ ffcc195 已推（0/0）。核心实测：Trimmomatic ILLUMINACLIP keepBothReads 字面参数被 Boolean.parseBoolean 解析为 false，R2 丢 1025/4029/8095 条；cutadapt 召回 98.3-98.4%（5-9bp 短箱仅 93.40%）、-O 3 误剪 ~2%。进度 6/7，仅剩 043（gp-043b lite 在跑真实迷你库路线）。

## read-qc 模块全量收尾（21:5x）
- 043 到货→独立复核通过→b1f30ff 已推。**read-qc 模块 7 篇（038-044）全部真跑收尾入库**。
- 站点重建：read-qc 8 页面（index+7 skill）+图资产到 assets/，验证通过；TRIALS/FAMILY_SUBS 注册提交。
- 卡片：7 份摘录稿（card-ms/read-qc/，从各小红书正文取真跑数字，无图引用故未绑 --assets）→ 前台 python -c 编排器 28/28 OK，每主题 3 张=12 张/组（合规）。发布台重建 7 条 read-qc 全就位（imgs/title/body/md 全有）。
- 本模块踩坑沉淀：git Bash 内联 wsl 命令 $ 提前展开（假结果）→ 一律 .sh 文件；wsl VM 中途重启冻伤 conda 半成品环境；setuptools 84 移除 pkg_resources（umi_tools 需 <81）；py3.13 只能解析到坏 umi_tools 0.2.3 → 专开 py3.11 环境；fastq_screen 包名带连字符；根目录 0 字节 U+F022 垃圾文件=重定向弯引号污染。
- 遗留：bio-umi 环境的 `umi_tools version` 子命令损坏属已知怪癖，用真实子命令验证。

## genome-intervals 模块开工（22:xx）
- 拍板：read-qc 收尾后进 genome-intervals（8 skill：045 bed-file-basics / 046 bedgraph-handling / 047 bigwig-tracks / 048 coverage-analysis / 049 gtf-gff-handling / 050 interval-arithmetic / 051 overlap-significance / 052 proximity-operations，编号 045-052 原空闲）。
- 工具：bio 环境补装 pybedtools 0.12.1 / gffread 0.12.9 / ucsc-bedgraphtobigwig v2.10 / bigwiginfo / bigwigaverageoverbed（EXIT=0 验证过）；bedtools v2.31.1 + samtools 1.24 原有。
- 8 个 lite agent（gi-045…gi-052）两波并行，prompt 沿用 read-qc 模板 + .sh 纪律 + ASCII 引号纪律。
- 站点注册已就位未提交：TRIALS 045-052 + GENOMEI_SUBS + FAMILY_SUBS；发布台 CATS/CAT_CN 加 genome-intervals（genome-intervals 本就在 DOMAIN_OF"基因组与变异"组）。

## genome-intervals 模块全量收尾（23:xx）
- 8 篇（045-052）全部真跑收尾、独立复核、入库推送（末批 f922327 + 站点注册 dacf427，0/0）。
- 亮点实测：045 覆盖恒等式/三类静默失败；046 深度比 3.99x RPM 对齐+错误契约四项；047 basesCovered 零误差+宽窗稀释；048 对账比值全 1.000+-pc 整对丢失坑；049 CDS +3nt 约定+start-1 过度校正；050 40/40 对账+-split 3.7 倍+-sorted 10.6 倍；051 fisher 全基因组零假设伪显著 p=4.5e-26 vs workspace 置换正确；052 closest 72/72+-t all 平局+-D ref 链反 30 个。
- 卡片：8 份摘录稿（card-ms/genome-intervals/）→ 32/32 OK 每主题 3 张；发布台 genome-intervals 8 条就位。
- 至此五大模块（alignment/read-alignment/alignment-files/variant-calling/read-qc/genome-intervals 中前五个）全部闭环,共 35 篇真实试用。
