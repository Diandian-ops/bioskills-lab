# 2026-09-01 工作日志

## 015 小红书卡片图补齐（4 主题全生成）
- 用户"行 你来整"：给 variant-calling/015 补出 4 主题 md2card 小红书卡片图。
- 真相：notebook 主题 14 张图其实 8/31 20:06 已生成（此前 ls 误判"不存在"），缺的是 minimal/research/editorial。本次用 PowerShell+反斜杠路径重跑全 4 主题（notebook 覆盖刷新，保证 4 主题一致+素材绑定统一）。
- 流程：先起 md2card 服务（content/库/md2card `npm run dev` → 5173，VITE v6.4.3 ready），再用 venv python 跑 `md2card_patch.py <真实试用md> --theme <t> --cover standalone --out output/xhs-site/xhs-cards --url http://localhost:5173/app/ --hd`（t=notebook/minimal/research/editorial），最后 `build_xhs_console.py` 重建 index.html。
- 产物：notebook 14 张、minimal/research/editorial 各 13 张 PNG + README.txt；素材目录 `content/素材/variant-calling/015-variant-calling` 自动绑定（fig1/2/3 进卡片）。
- 验证：经 8899 服务 4 主题 01.png 全 200；重建日志 `015 [variant-calling] imgs=14`；index.html 已写入 015 卡片数据。

## 两个踩坑（可复用，已固化 MEMORY.md）
- 坑1 Git Bash MSYS 路径转换：Bash 里 `/d/1.WorkDir/RedBook/...` 被错转成 `D:\d\1.WorkDir\RedBook\...` → `md2card_patch.py` 找不到。改用 PowerShell（不转换路径）+ 反斜杠 Windows 路径。
- 坑2 /api/regen JSON 中文路径：`serve_console.py` 的 `/api/regen` 用 PowerShell `ConvertTo-Json` 传中文 md 路径时，中文在 Windows 下被损坏 → `os.path.isfile(md)` False 报"出图源稿不存在"。改直接用 venv python argv 直传中文路径（无 JSON 编码），绕过接口。

## 全量卡片完整性审计 + 批量补齐（用户授权"补齐全部缺失主题"）
- 审计 24 篇 xhs-cards：仅 **004（四主题全齐）** 与 **015（刚补齐）** 完整；其余 **22 篇都缺主题**——大多只有 notebook，read-alignment 011–014 多一个 editorial，minimal/research 几乎全缺。合计约 50+ 个主题待生成。
- 批量脚本：`output/xhs-site/logs/batch_regen.ps1`（gitignored）。逻辑：扫 `xhs-cards/<cat>/<slug>/`，对每个缺失主题用 venv python 跑 `md2card_patch.py`（**断点续跑**：已存在带 png 的主题跳过），每个主题独立日志落 `output/xhs-site/logs/batch_regen_<时间戳>/`，全程 Ensure-MD2CARD 保活 5173，结束统一 `build_xhs_console.py` 重建并汇总 done/skip/fail。
- 启动：PowerShell 后台 task `087xnc`（约 30–45 分钟）。md2card 服务（5173，后台 Bash task `L0rikf`）需全程在线。
- 预期结果：24 篇全部 notebook/minimal/research/editorial 四主题齐全，发布台主题样张网格四主题均显示真实封面。

## 批量补齐最终收尾（24/24 达成）
- **结果**：`COMPLETE4THEME=24/24`，`REMAINING_THEMES=0`，`WRONG_DIRS_LEFT=0`。共补齐 **37 个缺失主题**（008 research；009–027 minimal+research；024–027 editorial）。`build_xhs_console.py` 重建 `index.html`（563 KB，注册 004–027 全部 24 篇，各带 imgs 计数）。
- **跑通方法**：后台/前台 PowerShell 跑含嵌套循环的长编排脚本会**随机 1s 空转 no-op**（只打印枚举行+CHUNK_DONE、零 GEN），不可靠。最终改用**前台 `python -c` 内联编排器**直接 `subprocess.run([VENV, PATCH, md, --theme, ...])` 逐个主题出图，`os.listdir` 判 PNG/无 `.nometa.md` 决定跳过，一次跑完 37 主题（`gen=37 ok=37 fail=0`），前台 python 不受 120s 后台上限约束、代码不读文件（不走 overlay，规避 Errno 9）。此为批量补齐首选路径（已固化 MEMORY 坑11）。
- **踩坑与修复（本段新增，已固化 MEMORY 坑3–坑11）**：
  - 枚举 `content/笔记/<cat>/*.md` 会混入 `小红书` 版 md → 给错误版本出图并污染 xhs-cards 目录。**必须 `Where-Object { $_.Name -like '*真实试用*' }`**，已生成的 `小红书` 错误目录用 `find <xhs-cards> -type d -name '*小红书*' -exec rm -rf {} +` 清掉。
  - PowerShell 5.1 `Join-Path $A $B $C $D` 只认前两个参数 → 校验路径错、进度卡死。改用嵌套 `Join-Path`。
  - `md2card_patch.py` 导出前 `wait_for_function(timeout=20000)` 对这些笔记恒 hang（每主题白等 20s，120s 窗仅 ~4 主题）；已将 `timeout=20000` 降到 `3000`（gitignored 的 output/ 脚本），每窗产能约 ×5。
  - 后台任务 ~120s 硬上限 → 分窗续跑；每窗前清 `.nometa.md`/`.zip` 临时稿，且「完成」判定须同时要求「有 png 且无 .nometa.md」，否则 cap 中途杀进程留下的临时稿会让已完成主题被重做。
- **8899 验证**：当前 8899 端口被另一服务占用（根目录是 git 仓库列表，非 xhs 发布台），`/xhs-site/` 返回 404 属正常——xhs 预览须由 `launch_console.bat` 单独起 `serve_console.py`。文件侧已验证：index.html 在盘、`024 editorial 01.png` 等补齐卡片均在位。
- **临时预览 8900**：由 `serve_console.py 8900` 起的临时后台进程已按 120s 上限被回收（task `0oz0qq` failed），**属预期**；交付物（24/24 卡片 + index.html 563KB）在盘不受影响。

## 用户质疑「表述是否如实」→ 自我修正 + 确立表述纪律
- 用户问：汇报的观点和结论是否基于实际依据、而非脑补。逐条自查后分层答复：硬证据（命令 stdout / curl 状态码 / `os.listdir` 统计 / 脚本 SUMMARY）成立；**推断类结论有 3 处需标注**。
- **已修正一处事实错误（坑10）**：此前记录「PowerShell 5.1 `Join-Path` 传 4 参只认前两个、静默丢弃其余」是**未经实测的机制臆测**。实测（PSVersion 5.1.26100）真实机制是：抛 `ParameterBindingException: 找不到接受实际参数"C"的位置形式参数`；因脚本设了 `$ErrorActionPreference='SilentlyContinue'` 异常被吞 → 返回 `$null` → `Test-Path $null` 空 → `Has-Png` 恒 false。现象结论（路径算错、已完成主题被重做）仍成立，机制描述已更正，并加教训：批量脚本不要用 SilentlyContinue 掩盖参数绑定错误。
- **标注两处「根因未查明」**：① PowerShell 长编排脚本「1s 空转 no-op」（打印了 `REALTRIAL_SLOTS=7` 却零 GEN）无法用坑10 解释，"flaky"只是现象标签；② 坑4 "overlay"、坑5 "安全扫描拦截" 均为**模型推断的机制标签**，底层真实原因未验证。
- **新确立表述纪要（写入 MEMORY.md）**：汇报结论必须能指出证据来源；无实测支撑时只能写**现象**并注明「根因未查明」，禁止把推断写成机制。

## 016–027 笔记可信度核查（用户要求「从 016 向后都核查一遍」）
- **结论：12 篇均未发现无出处编造**。命令/参数全部可溯源到对应 SKILL.md；数据结论均来自各自 `repro_transcript.txt` 真实运行输出；未执行部分均已标注。
- **方法**（已固化为 skill `bio-note-fidelity-audit`）：① 全文扫描命令行 → 按 tool+子命令、长选项 flag 回查 SKILL.md；② 查证据链（素材目录 + repro_transcript.txt 是否存在）；③ 查未执行是否诚实标注；④ 查主观绝对化词；⑤ 查「SKILL.md 原文 vs 本机实测」偏差是否双方都写明。
- **关键教训**：机器初筛不可直接采信——本次标记 5 项「无法溯源」，人工核实后 **4 项误报**（环境版本说明行被当命令、`gatk VariantFiltration` 因换行未连续匹配）。另一坑：Bash `cat` 读中文路径文件显示为空，须用 python `io.open` 读（020 transcript 因此一度误判为空）。
- **需披露的三处**：① 025 的 `samtools faidx` 管道法是 skill 未提及的替代方案（bcftools 1.24 consensus 不接受 `-r`），但已标注冲突原因 → 属合理工程适配，非编造；② 016 无独立「未覆盖」章节，但结尾段落已等价披露「相关结论按 SKILL.md 原文陈述、未以构造数据演示」；③ 022/023/027 无素材目录（未真跑所致，已标注）。
- **023 有 1 处修辞词**：第 32 行「该变异显然是真实的」——属概念转述修辞，非事实断言。
- 报告落盘：`output/audit/笔记可信度核查-016-027.md`（不入库）；核查脚本 `pipeline/audit/audit_note_vs_skill.py`（入库，可复现）。

## 三处整改（016 补章节 / 022·023·027 补跑尝试→转为难度说明）
- **016 已补「未覆盖（诚实标注）」章节**：结构化列出 gVCF/多等位/相位/符号等位/缺失基因型五类该输入无对应记录的项目，与原结尾等价披露合并，格式对齐其余 11 篇。
- **022/023/027 补跑：探测后用户拍板止损**（原话「如果真的实现困难 就详细介绍skills的内容好了 如实说明复现的难度即可」）。探测结论：
  - 工具链**不是**障碍：GATK 4.6.1.0 GitHub release 667MB 直连下载成功、JDK17 TUNA 190MB 成功、msys2 有 bcftools 1.24 包。
  - **真卡点是输入 BAM**：1000G 比对文件已全面迁 GRCh38DH（每样本全基因组 CRAM 8–15GB，无 chr 子集）；旧 GRCh37 BAM 路径（data/<POP>/<SAMPLE>/alignment/ 与 ftp/data/）实测 404；可行路径只剩「crai+远程 range 切 CRAM」，需 samtools+GRCh38DH 参考（950MB）且整链路未验证。
  - DeepVariant 官方仅 Docker/Linux 二进制，Windows 原生不存在 → 结构性不可行。
  - WS L：本机未安装（实测）。
- **整改动作**：三篇各插入「复现难度评估（2026-09-01 环境实测）」节（工具可获取性表 + 输入数据结构性困难 + 结论）；027 属性表 Docker 行按今日实测修正（初稿记「docker 存在但守护进程未跑」与今日「不存在」不符，已注明以今日为准）。三篇小红书版核查后无需改动（均已带诚实限定语）。gate_lint 三篇全 ERROR=0 WARN=0。
- **遗留**：`pipeline/022-tools/downloads/` 留有已下载的 gatk-4.6.1.0.zip(667MB)、jdk17.zip(190MB)、chr22.fa.gz(3.6MB)，gitignored；删除与否待用户定。若未来部署 Docker/WSL2 并解决 BAM 获取，可按笔记内命令模板真跑。

## 004–015 保真度核查（延续「继续」指令）
- **结论：12 篇均无编造**。标记 5 项 → 4 项误报（版本/叙述行被当命令）+ 1 项为已声明范围外的前置步骤（005 `MAFFT --auto`：第 13/28 行明确「比对构建由 multiple-alignment skill 覆盖，不在本 skill 范围内」）。
- 6 篇无「未覆盖」标注（004/005/011/012/013/015）实为**全跑通**：小结均写明真跑范围与未实测边界（如 012「GRCh38 ALT/decoy 与 -M/Y SV 语义按文档转述未实测」）。
- 历史制式差异（不构成违规，如实记录）：004–015 无 repro_transcript.txt / make_figs.py 自带（016 起才要求）；011–013 数据为 **wgsim 模拟读段 + 合成参考**（与 015 起「真实公开数据」取向不同，笔记内明示来源）；015 用 samtools 官方测试夹具。
- 001–003 为早期随笔/拆解，无单一 SKILL.md，按约定不参与核查、保持不动。
- skill 脚本 TOOLS 白名单已扩充 MSA/比对类工具（mafft/muscle/trimal/iqtree/bowtie2/bwa/hisat2/star 等），否则 004–014 会 cmds=0 误判。
- 报告：`output/audit/笔记可信度核查-004-015.md`。至此 004–027 全部 24 篇核查完毕，累计两份报告 + 两份初筛明细。

## 全量收尾：gate_lint 彻底全绿 + 站点重建
- 全量回归发现 5 个**存量** WARN（004 FANER+ABBR、005/006 ABBR、011 标题区 ADJ_REPEAT）——此前 8/29 基线记录「全 0」与实际不符（记忆纠偏：以后基线声明须以当次全量实测为准，不凭旧记录断言）。
- 已修复 5 处：004 PID 加注（Percent Identity）+ 去掉"反而"；005 SP-score 加注（Sum-of-Pairs）；006 MSA 加注（Multiple Sequence Alignment）；011 META 标题改为与主标题差异化（bowtie2 真实试用：…+49pp…）。复跑全量：**24/24 ERROR=0 WARN=0 彻底全绿**。
- `pipeline/build_lab_site.py` 重建站点（EXIT=0）：022/023/027 子页各含「复现难度评估」×1、016 子页含「未覆盖（诚实标注）」×1、004 子页含 Percent Identity 注释——笔记改动已全部进站。

## 清理与 skill 入库（用户确认执行）
- `pipeline/022-tools/`（861MB：gatk zip 667MB + jdk17 190MB + chr22.fa.gz）已按用户「删了吧 暂时用不到」删除。
- **bio-note-fidelity-audit 已同步进 workbuddy-skills 库**：本地库 `D:/1.WorkDir/workbuddy-skills`（本次新 clone，remote=Diandian-ops/workbuddy-skills.git，单 main 分支、skill 平铺目录惯例）；commit `165c6c4` 已推送 main。**坑**：本机所有 git 仓库均无 user.name/email config（全局也没有），历史作者统一为 `Diandian-ops <Diandian-ops@users.noreply.github.com>`——新 clone 的库提交前须手工设 local identity，否则 fatal: empty ident。

## 用户授权处理核查三处遗留 + 新增强约束「小红书 ≤18 张」
- **已完成：016 补「未覆盖（诚实标注）」章节**（插在「5 实践要点」与「6 小结」之间），列出 5 类未实测记录：gVCF `<NON_REF>` 与参考区块、多等位 `1/2`、相位 `0|1` 与 PS、符号等位 `<DEL>` 与 END、`./.` 缺失基因型；均注明「按 SKILL.md 原文陈述，未以构造数据演示」，并说明后续若有相应数据可复用第 4 节命令组补齐。
- **⚠️ 重大环境发现：生信工具链已丢失**。全盘搜索无 `bcftools` / `samtools` / `bgzip` / `tabix`；无 conda/mamba/miniforge 目录；WSL 未安装（无分发版）；PATH 无生信目录。但素材 transcript 显示 2026-08-28 尚在 `bcftools 1.24` 环境跑通 → **工具链在 8/28 之后被清理**。影响面：① 022/023/027 补跑无基础链；② 已跑过的 9 篇素材 `make_figs.py`（依赖 bcftools）当前也不可复现。
- **补跑 022/023/027 可行性探测**：java 仅 IDE 内置 jre21（`C:\Users\Admin\.antigravity\extensions\redhat.java-1.52.0-win32-x64\jre\21.0.9`）；docker / singularity / apptainer 均无；网络 github 返回 000（不通）、broad 返回 200（通）。→ DeepVariant 官方仅 Docker/二进制分发，无 Docker 基本不可行；GATK 需 jar（GitHub 不通）+ Java + 参考基因组 + BAM。
- **新增强约束（用户补充）**：小红书**最多 18 张图**，用户心理预期 **10–16 张**。实测 24 篇×4 主题共 96 组，张数 8–22（均值 12.4）：**016 = 22 张（超硬上限，发不出去）**、021 = 17 张（超预期）、024/026/027 = 16 张（边界内）、012/013/018 = 8–9 张（略低于预期但可发）。
- **坑：`--density` 参数对张数无效**。实测 016 四主题 `balanced` → `compact` 重跑，张数完全不变（22→22、20→20），说明 md2card 分页由**内容长度**决定，不由密度档位决定。→ 降张数只能精简源稿，调 density 无用。
- **更正**：此前称 016「有 30 个代码块」是错的——`grep -c '^```'` 数的是围栏行（30 = 15×2），实际为 **15 个代码块、57 行、占全文 20%**。22 张源于 290 行全文总量，非代码块。

## 张数整改落地（小红书 ≤18 硬约束）
- **方案：卡片专用摘录稿**。落点 `output/xhs-site/card-ms/<cat>/<原slug>.md`（父目录名即 cat；文件名=原 slug 以便**覆盖**原卡片），出图时加 `--assets <素材目录>` 显式绑图（因摘录稿不在 `content/笔记/<cat>/` 下，自动推导会失败）。**正式笔记一字不动**，深拆内容完整保留。
- **016：22 → 11 张**（四主题全 11）。摘录法：保留统领原则、属性表、四种置信度表、Number 语义、四条实测论断 + 3 张图、实践要点、未覆盖、小结；代码块 15 → 3，删 3.1 文件结构与 cyvcf2 输出演示。
- **021：17 张，未动**（删 13 行未跨分页阈值，仍是 17）。理由：17 ≤ 18 硬上限、能发；继续删会伤到 csq 后果分布与多转录本歧义（53.6%）等核心内容。
- **全量复核：96 组，8–17 张，均值 12.0；超 18 = 无**（此前 016 的 22 张已消除）。发布台已重建。
- **重大坑：出图脚本不清理旧图**。改稿重出后目录残留旧 PNG，导致 `ls | wc -l` 虚高（看着仍 22 张，实际新稿只 11 张）。→ **改稿重出前必须 `rm -rf <slug>/*-standalone-hd/`**，判张数以清空后结果为准；排查可用 `os.path.getmtime` 按时间戳区分新旧。

## 环境探测修正与工具链尝试
- **更正：GitHub 实为连通（200）**。此前 `curl --max-time 8` 返回 000 是超时误判，据此得出"GitHub 不通"的错误结论并写进记忆，已更正。教训：连通性探测超时不宜过短，且单次 000 不应直接判死。
- **miniconda 已安装**：`C:\Users\Admin\miniconda3`（`/D=` 参数未生效，落到默认位置；conda 26.7.1）。C 盘 83G 可用，够用。
- **bcftools 装不上**：conda-forge/bioconda **无 Windows 版 bcftools**（`PackagesNotFoundInChannelsError`）；Git Bash 无 pacman（精简 MINGW64，非完整 MSYS2）；pysam 无 Windows wheel（需编译）。→ 纯 Windows 原生无路，正道是 WSL（未安装，需管理员+重启，待用户决策）。
- **conda 26.7 新规**：默认渠道需接受 Anaconda ToS（`CondaToSNonInteractiveError`）。规避法：`conda create -n bio --override-channels -c conda-forge -c bioconda <pkg>`，完全不碰 defaults。
