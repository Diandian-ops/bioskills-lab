# 2026-08-25 工作日志

## 小红书笔记风格偏好（用户明确）
- 笔记不要运营互动话术：去掉「关注我 @XXX」「下期预告」「评论区抛问题/攒选题」这类引导。
- 只讲内容/skills 本身，结尾用中性收束即可。
- 已对 `笔记/003-bioSkills拆解01-alignment专题.md` 据此删去结尾互动段。

## 排版工具 DingCard（库/DingCard）
- 发现 lottshin/DingCard v0.15.0：小红书长文排版 + 轻设计出图 Web 工具（React/Vite）。Markdown→分页图文卡片，导出 PNG/ZIP；另有自由画布做封面/重点页。
- 对本项目价值：正好补上「md 文案 + 散图 → 可直接发小红书的图文」这最后一公里。
- 适配点（读 src/markdown.ts 确认）：
  - 独立行 `---`/`***`/`___` = 强制分页符（不是 hr）。笔记现有 `---` 章节分隔会被当分页，需按需清理或利用。
  - 图片用 `img:<id>` 短引用（imageStore 管理，base64 不写进 source）；浏览器应用读不了本地相对路径 `../素材/...`，需把图单独粘贴/拖入 DingCard 重新导入。
- 运行：在线 dingcard.vercel.app，或本地 `npm run dev`（Node 20+，本机有 22/24）。
- 2026-08-25 实测：本地 `npm ci && npm run dev` 成功（144 pkg, vite 5.4.21 ready），dev server 长驻于 **http://localhost:5173/**（task_id khDenR 后台运行），curl 验证 HTTP 200、标题「叮卡」。已用 present_files 打开内置预览。停服需 kill 该后台任务或 Ctrl-C。

## 笔记 → DingCard 命令行预处理（已实现）
- 脚本：`/tmp/preprocess_dingcard.py`（可复用，逻辑：正则把 `![alt](../素材/.../x.png)` 换成「在 DingCard 中拖入此图 + 绝对路径」占位，避免浏览器读不到本地路径显示破图；顶部配图说明补全绝对路径；`---` 分页符与表格/代码块原样保留）。
- 产物：`笔记/003-bioSkills拆解01-alignment专题_dingcard.md`（003 的 DingCard 友好版，4 张图已转拖入提示）。
- 用法：粘该 md 进 localhost:5173 左侧 → 右侧按 `---` 自动分页 → 到占位处从文件管理器把对应 png 拖入 → 调主题/字号 → 导出 PNG/ZIP。
- 局限：DingCard 无官方 CLI，纯命令行只能做到「预处理 md」；若要「命令行一键导出排版图」需 Playwright 驱动浏览器（DingCard 已带 @playwright/test，但需 `npx playwright install chromium` 且导出涉及点击+图片上传，待用户确认再做）。

## ✅ Playwright 全自动排版导出（已跑通！）
- **不需要下载 Playwright chromium**——直接用系统 Google Chrome（`channel: 'chrome'`），`/Applications/Google Chrome.app`。
- **图片注入方案**：
  - ❌ data URL 内联（base64 塞进 md）：文档膨胀到 555KB，marked 解析器把长 URL 当纯文本，图片不渲染。
  - ❌ 跨域 HTTP（localhost:8765）：预览正常但导出时 html-to-image canvas 被 CORS 污染，ZIP 生成卡死/超时。
  - ✅ **同源 HTTP（Vite public/ 目录）**：把 png 复制到 `库/DingCard/public/`，URL 用 `/xxx.png`（解析为 localhost:5173 同源）。文档仅 7KB，预览+导出均成功。
- **注入方式**：`window.__cmView.dispatch({ changes: { from:0, to:docLen, insert:md } })` → CodeMirror updateListener 自动触发 onChange → 刷新右侧预览。store 未暴露到 window（hasStore=false）。
- **导出**：`button.toolbar-primary`（"打包下载 N 页"）→ `page.waitForEvent('download')` 捕获 ZIP。
- **产物**：`产出/003-bioSkills-alignment-排版.zip`（24 张 PNG 卡片，3.6 MB）。
- **可复用脚本**：`库/DingCard/scripts/dingcard_export_003.cjs`（改 IMG_BASE_URL 和 NOTE_PATH 即可用于其他笔记）。
- **依赖**：DingCard dev server(5173) 必须在跑；图片需先复制到 DingCard public/；系统 Chrome 已装。

## 沉淀为 user-level skill
- 已创建 `~/.workbuddy/skills/dingcard-layout-export/SKILL.md`：封装「md笔记+图 → DingCard 自动排版导出小红书图文 ZIP」全流程，含三方案踩坑对比（data URL 膨胀/跨域 CORS 卡死/同源 public 成功）。
- 触发词：自动排版导出 / DingCard 导出 / 文章转小红书卡片。
- 用户 skills 仓库已 git 关联（Diandian-ops/workbuddy-skills），该 skill 位于 `~/.workbuddy/skills/`，需用户自行 `git add dingcard-layout-export && commit && push` 同步到远程（未自动 push）。

## ✅ 三篇笔记全自动排版导出完成
- **通用脚本**：`库/DingCard/scripts/dingcard_export.cjs`（参数化：`node dingcard_export.cjs <笔记路径> [输出ZIP]`；自动提取 `../素材/...` 图片引用 → 复制到 public/ 同源 → 替换为 `/xxx.png` → 注入 → 导出 ZIP）。
- **产出目录**：`产出/`
  - `001-生信AI-排版.zip` — 10 页纯文字卡片（1.3 MB）。**注**：001 的 4 张素材是早期 AI 插画（用户之前明确不喜），未擅自插入。如需带图文版需换真实数据图或重新确认。
  - `002-bioSkills-排版.zip` — 15 页含真实数据封面图 bioSkills-cover.png（2.2 MB）。card-04 渲染了 Top15 分类条形图。
  - `003-bioSkills-alignment-排版.zip` — 24 页含 4 张真实数据图 MSA/黄昏区/熵/算法（3.6 MB）。
- **002 笔记已修改**：在 `## 正文` 前插入了 `![](../素材/002-bioSkills/bioSkills-cover.png)` 引用（使通用脚本自动提取并渲染）。

## ✅ Skill 沉淀（笔记生成 + 发布稿清洗）
- **新建 `redbook-bio-note-writer`**（`~/.workbuddy/skills/redbook-bio-note-writer/SKILL.md`）：沉淀"小红书生信笔记内容生产"方法论——笔记结构模板（含 `<!-- META -->` 元信息标记）、风格约定（男作者/不堆互动/真实图）、bioSkills 拆解方法、配图规范。与 `dingcard-layout-export` 衔接形成完整链路。
- **更新 `dingcard-layout-export`**：新增「步骤 0：发布稿清洗」（剥离 `<!-- META -->...<!-- /META -->` 元信息段），脚本 `dingcard_export.cjs` 已内置该逻辑。

## ✅ 发布稿清洗落地（A 方案已执行）
- **元信息标记**：给 001/002/003 的写作元信息段加了 `<!-- META -->...<!-- /META -->` 包裹（标题备选/封面文字参考/系列说明/配图说明/参考仓库/发布顺序）。
- **发布就绪版 ZIP**（可直接发小红书）：
  - `001-生信AI-发布版.zip` — 9 页（剥离标题备选+封面参考）
  - `002-bioSkills-发布版.zip` — 14 页（同上 + 含 bioSkills-cover 图）
  - `003-bioSkills-alignment-发布版.zip` — 22 页（剥离系列/配图/参考仓库/发布顺序，保留正文+4张真实数据图）
- **验证通过**：003 发布版 card-01 只剩标题，card-02 直接正文，零元信息外泄。

## ✅ 排版专业性升级：套用「编辑档案」模板主题（已实现）
- **根因分析**：此前导出脚本未设定 DingCard 主题 → 默认「简约白」（白底黑字蓝强调）→ 用户反馈"太不专业/不够好看"。
- **DingCard 内置模板体系调研**（读源码确认）：
  - Markdown 工作区：3 个 template themes（编辑档案 / 公共剧场 / 议题封面）— 各带独立底色/强调色/字体/刊头页码 chrome
  - 自由画布工作区：4 个杂志级手动设计模板（编辑部 / 清单 / 信号 / 夜航）— 封面级，需手排元素
  - 主题由 `MarkdownWorkspace.tsx` 的 `<Select>` 控制（`aria-label="主题"`），本地 React state（不落 localStorage）
  - 卡片通过 `data-card-theme` 属性驱动 CSS 变量 + `MarkdownCardChrome` 组件渲染专业 chrome
- **导出脚本升级**（`dingcard_export.cjs`）：
  - 新增 Step 3.5：注入 md 后程序化点选主题 Select + 字体 Select（Playwright 驱动 combobox）
  - 默认主题 = `模板 · 编辑档案`（暖纸 #eee8db + 红强调 #dc3f2f + 宋体 + DING/ARCHIVE 刊头页码）
  - 默认字体 = `系统宋体`（Songti SC，macOS 系统自带）
  - 可通过环境变量覆盖：`DINGCARD_THEME`、`DINGCARD_FONT`
  - headless 默认改为 true（全自动友好）；设 `DINGCARD_HEADLESS=0` 可弹窗看效果
  - 输出文件名含主题 key（如 `-editorial.zip`）避免覆盖旧版
- **重新产出对照样张**：
  - `002-bioSkills使用体验-editorial.zip` — **12 页**，编辑档案风格，含 bioSkills-cover 图（3.2 MB）
  - `003-bioSkills拆解01-alignment专题-editorial.zip` — **23 页**，编辑档案风格，含 4 张真实数据图（5.6 MB）
  - 预览截图确认：纸感底色 + 刊头标签 + 页码 folio + 宋体标题全部正确渲染
- **Skill 文档更新**：`dingcard-layout-export/SKILL.md` 全面重写——新增主题一览表、千字长文+图+表格支持情况表、18 张图硬约束说明

## 用户两个关键问题的结论
1. **排版专业性**：✅ 已解决。从默认「简约白」升级为「编辑档案」杂志风（一键自动）。另有公共剧场/议题封面可选；自由画布模板可做精致封面。
2. **千字长文 + 图 + 表格支持**：✅ 全部支持。文字自动分页（~1000 字 ≈ 8-15 卡）、图片同源渲染、表格/代码/引用均 OK。但注意：(a) 小红书单篇 ≤18 图（003 的 23 页超标需拆篇）；(b) 宽表格在 3:4 卡片上会挤 → 建议用 matplotlib 出图替代。

## ✅ 精简到 3-6 张/篇（用户核心诉求）
- **用户要求**："精简 不要那么啰嗦能3-6张搞定一篇文章即可"
- **002 bioSkills使用体验**：12页 → 重写为 3 个核心段落 → **6 页** ✅
- **003 alignment拆解**：23页 → 4图合成1张2×2拼图(ImageMagick montage) + 极简文字 → **6 页** ✅
- **导出脚本升级**：
  - 新增图片自动 resize（copy 到 public 后 `sips -Z 600` 缩小高图）
  - 新增 Step 3.6 CSS 注入（`.card-content .img-wrap img { max-width: 400px }`）
  - 踩坑记录：HTML `<img width="N">` 标签绕过 DingCard `.img-wrap` 导致布局崩坏（16页）；纯 sips resize 无效（CSS `max-width:100%` 把窄图拉回全宽）；最终拼图+CSS 组合方案生效
- **"都是同一个图"问题排查**：素材 4 图 md5 全不同、public 落盘 4 图 md5 全不同、脚本替换逻辑正确（4 个独立 src）→ 4 张数据图视觉完全不同（MSA矩阵/S曲线/熵剖面/工具对比折线）。用户可能看到的是旧版缓存。
- **拼图文件**：`素材/003-alignment/003-grid-4up.png`（920×1200, ImageMagick montage 2×2 grid, 暖纸底色 #eee8db）
- **Skill 文档更新**：dingcard-layout-export/SKILL.md 新增「页数控制」章节，含实测对照表和拼图策略说明

## ✅ bioSkills alignment skill 真实试用（用户要求"拿 alignment 试试"）
- **环境准备**：managed venv 安装 biopython 1.88（隔离，不污染本机）；numpy 2.5.2 + matplotlib 3.11.1 已有
- **msa-statistics skill 试用**：
  - 原样跑 `entropy_analysis.py`（skill 参考实现）→ 跑通
  - 输入：bioSkills 自带 sample_alignment.aln（DNA, 4seq×21col）→ 每列去gap后单一字符 → 熵全0 = 正确结果（示例数据设计上100%保守）
  - 结论：脚本逻辑正确；但示例数据太平不适合出图
- **pairwise-alignment skill 试用（主角）**：
  - 用 NCBI Entrez 在线取两条真实蛋白序列：P69905(HBA_HUMAN α链,142aa) vs P68871(HBB_HUMAN β链,147aa)
  - 按 SKILL.md 教的 BLOSUM62+global+open=-11/extend=-1 标准配置比对 → score=286, align_len=149
  - **验证 skill 警告点①**：默认 gap=0 → align_len=172（多23个无意义gap），score反而更高(339) → 印证正确
  - **验证 skill 警告点②**：PID 四口径差异 pid1=43.6% / pid2=46.4% / pid3=45.8% / pid4=45.0%（此例2.8pct，skill说极端可达11.5%）
  - **产出两张真实数据图**（matplotlib 英文标签）：
    - `素材/004-pairwise/004-pairwise-status.png` — 逐列比对状态热图（match绿/mismatch红/gap灰）
    - `素材/004-pairwise/004-pairwise-identity.png` — 滑动窗口 identity 剖面（window=15, 40%阈值线）
- **试用结论**：skill 教的内容准确、reference 脚本能直接跑通、默认 gap penalty 警告是真实坑。可作为 004 笔记素材。

## ✅ 004 笔记：bioSkills 真实试用 pairwise-alignment（用户要求"深究写一篇"）
- **用户意图**："这种真实的试用和关键问题以及里面skills的解读 值得深究写一篇出来"
- **笔记**：`笔记/004-bioSkills真实试用-pairwise-alignment.md`
- **深度解读两个核心坑**：
  1. 默认 gap penalty=0 → 多 23 个假 gap (+15.4%)，score 虚高
  2. PID 四种口径（43.6%~46.4%，差 2.8pct），分母定义不同
- **配图策略**：2 张独立图（identity+status / PID+gaptrap）→ 合成 **1 张总拼图** `004-grid-all.png`（1540×1336 → resize 到 h=350）
- **页数控制历程**：11页(2独立图) → 9页(精简文字) → 7页(1总拼图+h=600) → 7页(h=450) → 7页(h=350) → **6页**(修复重复标题) ✅
- **导出脚本升级**：IMG_MAX_HEIGHT 从 600 降到 350（更紧凑适配小红书窄卡）
- **产出**：`产出/004-bioSkills真实试用-pairwise-alignment-editorial.zip`（6 页，1.67 MB）

## ✅ 005 笔记：bioSkills 真实试用 msa-statistics（严格按 skill 原方案复现）
- **用户纠正**："不行 我建议严格按照skills原本的方案来复现 没有包就用conda或者venv虚拟环境来创建在当前路径下" — 拒绝 pyalign（skill 外库），要求用 conda/venv 在项目路径建环境
- **严格复现流水线**：
  1. 项目路径建 venv：`/Users/zhangdiandian/RedBook/.venv`（biopython 1.88 / numpy 2.5.2 / matplotlib 3.11.1）
  2. conda 装 MAFFT：`/Users/zhangdiandian/RedBook/.env-mafft`（清华 bioconda 镜像，MAFFT v7.526）
  3. NCBI 取 8 条真实 globin 序列（HBA/HBB/HBD/HBG/MB_human/MB_bovin/HBA_mouse/neuroglobin）
  4. MAFFT --auto 对齐 → 158 列真实 MSA
  5. 原样运行 skill 的 `entropy_analysis.py` → 逐列 Shannon 熵 + KL 信息量
- **核心坑深究**：蛋白 IC 必须用 KL vs Robinson 1991 经验背景，不能套 DNA 均匀背景。完全保守的 Leu(常见) IC=3.44 vs Trp(稀有) IC=6.23，均匀背景错误地给两者都算成 4.32
- **配图**：1 张双面板图（A: 真实 MSA 熵/IC 剖面; B: uniform vs Robinson 概念对比柱状图）→ `素材/005-msa-statistics/005-fig.png`
- **笔记**：`笔记/005-bioSkills真实试用-msa-statistics.md`
- **页数**：**5 页** ✅（编辑档案模板，含 1 张真实数据图）
## ✅ 004 严格重做 + 005 补成分拆解（方法论升级）
- **用户方法论纠正**（三条）：
  1. "真实试用需要在正文里详细解析 skills 中的内容成分" → 笔记必须逐块拆解 skill 的文件结构/参考脚本/核心API/经验封装
  2. "不用在意 6 张的限制" → 不再压缩页数，让内容自然展开
  3. "004 也需要严格按照 skills 来进行复现 不要自己补充" → 只跑 skill 自己教的方法/代码，不加 skill 外的分析
- **004 严格重做**：
  - 删除了"自己补充"的内容：滑动窗口 identity 剖面、逐列状态热图（skill 不教这些）
  - 保留的图：仅 skill 点名的两个坑的可视化（gap=0 陷阱柱状图、PID 四口径柱状图）
  - 新增：完整 Skill 成分拆解章节（文件结构/7个文件/每个脚本干什么/核心API/知识封装）
  - 复现脚本 `实验/004-pairwise-redo/reproduce.py`：严格用 skill 的 protein_alignment.py 配置 + alignment_from_file.py 读 FASTA 模式
  - 实测数据：默认 gap=0 → align_len=172/gaps=55 vs 推荐 -11/-1 → align_len=149/gaps=9（多46假gap）；PID 四口径 43.6~46.4%（差2.8pct）
  - **页数**：**24 页**（编辑档案模板，不限页数）
  - **产出**：`产出/004-bioSkills真实试用-pairwise-alignment-editorial.zip`（5.9 MB）
- **005 补成分拆解**：
  - 原版(5页)缺少 skill 成分拆解，直接跳到"怎么试的"+"核心坑"
  - 重写后新增：skill 是什么/文件结构(1+8+1)/8个参考脚本逐一说明/核心API链/两大坑+BLOSUM62 Array 警告
  - **页数**：**20 页**（编辑档案模板，含 1 张双面板真实数据图）
  - **产出**：`产出/005-bioSkills真实试用-msa-statistics-editorial.zip`（5.5 MB）

## ✅ 实践结果共享方案确定 + 交互式 HTML 实验室首页
- **用户核心诉求**："小红书大家只能看到这些过程不能自己来进行，有什么方法我可以把实践的结果共享给大家" → 需要把可复现产物（代码/数据/环境）共享出去
- **共享载体确定**（读 WorkBuddy「资料库」skill 确认）：
  - 腾讯资料库 = WorkBuddy 原生资料库 workbuddy.cn/space，支持发布 `kind=page` 的 HTML、生成 `*.workbuddy.link/p/{id}` 可分享链接
  - 腾讯文档长文版（已连接）作为文字深读补充
  - GitHub 公开仓库（Diandian-ops，gh 已登录）作为技术同行 clone 复现通道
- **最终形态选择**：HTML 方式交付 → 比纯图文强（读者浏览器直接看完整实践，无需 clone/装环境），比纯 GitHub 普适
- **交互式聚合首页 `产出/bioSkills-lab.html`**（345KB 自包含）：
  - 生成器：`实验/build_lab_html.py`（图片 base64 内嵌、代码从文件读、文字 hardcode）
  - 内容：004 pairwise-alignment + 005 msa-statistics 两个 DEEP DIVE section（skill 文件结构拆解 + 真实数据图 + 完整复现代码 + 实测结论）+ 预留其余 5 个 skill 扩展位
  - **交互特性**（用户要求"聚合交互网页"）：固定侧边 TOC + 顶部导航、锚点平滑跳转、当前章节高亮(IntersectionObserver scrollspy)、每段代码悬浮复制按钮、highlight.js 语法高亮、回到顶部按钮、响应式（移动端隐藏 TOC）
  - 视觉：编辑档案风格（暖纸 #eee8db / 宋体标题 / 红强调 #b5341f），与小红书图文同调性
- **阶段二待确认（远端动作）**：是否把此 HTML 发布到资料库拿 workbuddy.link 分享链接（印到小红书图文引流）。用户尚未确认发布。

## ✅ HTML 实验室从 alignment 单类升级到 bioSkills 总层面
- **用户纠正**："这个设计好像需要优化 应该是站在 bioSkills这个总层面来做" — 之前只站在 alignment 一个 category，应从 bioSkills 总仓库(66 category/562 skills/archived)组织
- **重构 `实验/build_lab_html.py`**：
  - 新增 bioSkills 总览区块：定位(GPTomics/bioSkills 教 AI coding agents 生信)、规模(64 category/562 skills)、archived 说明、方法论
  - **自动扫描仓库生成类别地图**：Python 运行时 os.listdir 扫 `库/bioSkills/` 所有 category + skill 数，生成 64 个 chip 网格(alignment 高亮 chip-done，其余灰)
  - alignment 降为第一个 category section(含 004/005 两个 DEEP DIVE 子节)，其余 63 category 占位待补
  - TOC/topbar 重构为总层面导航
- **产出**：`产出/bioSkills-lab.html`(351KB 自包含)，grid 左固定 TOC + 右内容，复制按钮/高亮/scrollspy 保留

## ✅ 巨无霸单页拆分为多文件静态站点 + 左侧栏 + 新配色
- **用户纠正①**："这么多内容都在一个页面中吗？？？？ 是否应该多个文件 加上链接 类似网站？" → 拆成多文件静态站点
- **用户纠正②**："每个part应该是左侧导航栏 整个网站的配色需要优化 目前的不好看" → 每页固定左侧导航树 + 重做配色
- **重构 `实验/build_lab_site.py`（替代 build_lab_html.py 单页版）**：产出 `产出/bioSkills-site/`（多文件）：
  - `index.html`(11KB) 总览+类别地图 / `alignment/index.html`(5KB) 分支页 / `pairwise-alignment.html`(13KB)+`msa-statistics.html`(15KB) 深度篇 / `style.css`(5KB) 共享 / `assets/` 3张图共享
  - **每页左侧固定深色导航栏**（站点树：首页 / alignment(展开 2 子页) / 其余 63 category 灰态），当前页高亮；移动端隐藏侧栏
  - **新配色**：深色侧栏 #1b2330 + 白内容 #fff + 靛蓝强调 #6366f1（替代原暖纸#eee8db+红#b5341f 的"编辑档案"土味风），indigo 数字卡 / indigo 填充 chip / github 浅色代码块
  - 自动扫描 bioSkills 仓库生成 64 category 地图；DONE={"alignment"} 高亮
  - 本地预览：python -m http.server 在 产出/bioSkills-site（端口 8099/8100）
- **交付形态建议**：多文件站点最适合 GitHub Pages（git push → *.github.io 真网站，手机可读可搜），优于资料库单页。待用户确认是否建 Diandian-ops/bioSkills-lab 仓库并开 Pages

## ✅ 侧栏多层级目录 + 主内容区居中（最新一轮优化）
- **用户纠正**："侧边导航栏应该有多层级目录 主内容区的文档应该居中"
- **`实验/build_lab_site.py` 重写**：
  - 侧栏改用 `<details class="navgrp">` 实现**多层级可折叠目录树**：每个 category 一个 `<details>`，二级列出该 category 下所有 skills；alignment 默认 `open` 且 `summary.cur` 高亮，下挂 2 个真实子链接(pairwise-alignment/msa-statistics)；其余 63 category 折叠 + 二级 skills 灰态 `nav-dis` 占位
  - 共 64 个 navgrp（= 64 category），顶部 `BIO / LAB` 品牌 + "实验室首页" 回链
  - **主内容区居中**：`.content{margin-left:264px}` 让出侧栏宽 + `.wrap{max-width:760px;margin:0 auto;padding:38px 28px 90px}` 内容居中
  - 移动端：`.menubtn`(☰) + `.side{transform:translateX(-100%)}` 抽屉式滑出，JS toggle
- **重新生成**：`产出/bioSkills-site/`（index.html 42KB / style.css 7KB / alignment 分支页 36KB / 004 44KB / 005 46KB / assets 3图）
- **本地预览**：python -m http.server 8123 于 产出/bioSkills-site（HTTP 200 验证通过）
- 已验证：64 个 details 折叠层级 ✅、`.wrap` 居中 ✅、移动端抽屉 ✅

## ✅ 类别地图升级为领域着色 Treemap（最新）
- **用户诉求**："类别地图是不是应该做思维导图或其他形式，用户看到马上清楚有哪些内容、各归属哪些领域" → 需求=领域分组 + 体量感 + 一眼可读
- **决策（frontend expert）**：选 **D3 Treemap** 而非放射思维导图——64 叶子节点的思维导图小屏标签糊、不可读；Treemap 面积=skill数(体量)、颜色=领域(分组)、悬停明细、点击跳转，更贴合诉求且更有"领域地图"记忆点
- **领域分类法（domain taxonomy）**：把 64 category 归入 10 个领域（序列基础与读取/转录组与差异/单细胞与空间/表观与调控/基因组与变异/蛋白与结构/微生物与生态/多组学与系统/免疫与临床/数据与工具），每域独立配色
- **实现（改 `实验/build_lab_site.py`）**：
  - 新增 `DOMAIN_META`(10 域 名+色) + `DOMAIN_OF`(64 category→域 映射) + 自动聚成 `TREE` JSON（域→category→value/done/link）
  - 首页"类别地图"段替换为：SVG 容器 `#tm` + D3 v7(CDN) treemap 渲染 + 悬停 tooltip(`#tm-tip`) + 10 域图例(`.tm-lg`)；叶子按域色 brighter 渐变（同色系分组清晰），alignment 红框+✓ 可点击跳 alignment/index.html
  - 新增 CSS：`.tm-wrap/.tm-tip/.tm-legend/.tm-lg`
  - 注：站点内 skill 计数用**子目录数**(NTOTAL=562)，与 stats 卡片口径一致（非 .md 文件数 1186）
- **验证**：JS `node --check` 语法 OK；D3 CDN HTTP200；Node 实跑真实 D3 布局=64 叶子 0 坐标异常、`brighter().formatHex()` 正常、top3 大块=crispr-screens/comparative-genomics/variant-calling；本地 server 8123 已服务新版（HTTP200）
- 预览：**http://localhost:8123**

## ✅ 侧栏中文领域分组 + Treemap 去文字清理（最新一轮）
- **用户三点反馈**：① Treemap 乱乱的（疑因文字过长）；② 左侧导航栏应有中文；③ 按"领域全景"做好分类
- **根因确认**：旧侧栏在首页就把 64 个 category 的全部 skill 灰态全列出来（几千个 `<span>`），本身极重；Treemap 把 64 个英文长名+×数量塞进小格→糊
- **侧栏重写（`sidebar()`）**：改为 **领域→类别→skill 三级树**，与 Treemap taxonomy 对齐
  - 10 个领域 `<details class="navgrp">`，中文领域名 + 配色点(`.dot`) + ×skill总数
  - 领域内每个 category：active 的才展开显示 skills(灰态, 上限24+省略)；非 active 的仅 `<div class="navcat">` 紧凑标签(配色点+名+计数)
  - alignment(DONE) 恒为 `<details class="navsub">`，展开显示 2 个真实子链接(pairwise/msa) + 其余 skill 灰态
  - 首页仅 5 个 nav-dis（alignment 剩余 5 skill），旧版几千个 → 巨大瘦身（index 46KB→21KB）
  - 新增 CSS：`.navgrp>summary`flex+`.dot`、`.navsub`、`.navcat`
- **Treemap 清理（TREEMAP_JS）**：① 画布 560→600 加高；② 标签阈值 `w>60&&h>34` 提升到 `w>92&&h>46` 且**只显示 category 名(去掉格内 ×数量)**，小格成干净色块、明细走悬停
  - 实测(920×600 真实 d3 布局)：64 叶中仅 17 个大格显示名称(均为高体量 category)，47 个纯色块 → 清爽且信息量保留
- **验证**：JS `node --check` 通过；d3 布局 64 叶 0 异常；首页 10 中文领域组 + 63 navcat + 73 配色点；server 8123 服务新版(HTTP200)
- 预览：**http://localhost:8123**

## ✅ 类别地图 Treemap → Domain Catalog（解决小领域"显示不齐全"）
- **用户反馈**："微生物与生态 · 20 显示都不齐全" — Treemap 里该小领域(20 skill, 最小档)被压成细条，其下 3 个 category 格子均 < 标签阈值，名字全没显示，只剩色块+域头
- **根因**：面积 Treemap 天生不适合"把 64 个名都看清"，小领域必然被压成无名色块
- **决策**：放弃 Treemap，改 **按领域分组的完整目录（domain catalog）**——每块面板=一个领域(中文名+配色点+×skill数+体量色带)，面板内完整列出该领域全部 category（标签 chip；alignment 红框可点跳 alignment/index.html）
- **实现（改 `实验/build_lab_site.py`）**：删 TREEMAP_JS + d3 CDN；新增 `dom_totals/maxdom/catalog` 生成 10 个 `.dom` 面板（色带宽度=域总量/maxdom%，保留体量感）；纯 HTML/CSS 无 JS 依赖；CSS 由 `.tm-*` 换为 `.catalog/.dom/.dom-bar/.ctag` 等
- **验证**：10 面板 ✅、64 category 标签全列出 ✅、D3 引用 0 ✅、微生物与生态 3 类别齐全 ✅、alignment 红框可点 ✅；主页 19KB(原46KB)
- 预览：**http://localhost:8123**

## ✅ 回退：保留 Treemap，仅优化标签（用户纠正）
- **用户纠正**："我觉得这样不好 Treemap 我挺喜欢的 只是需要优化就可以了" — 用户其实喜欢 Treemap，反对换成 catalog；应保留 Treemap 并优化标签渲染，而非替换
- **动作**：撤销 catalog，恢复 Treemap（TREEMAP_JS + d3 CDN + .tm-* CSS），改进叶子标签逻辑：
  - 标签阈值从 `w>92&&h>46`(仅17格有名) 放宽到 `w>46&&h>24`(58格有名, 91%)
  - **只显示 category 名（去掉格内 ×数量）**，避免两行文字显乱
  - **按宽度自适应字号 + 超长截断('…')**，绝不溢出
  - **窄高格(h>1.5×宽)竖排文字**，让长名沿高度完整显示（如 metagenomics/microbiome 竖排完整；ecological-genomics 19字符竖排仍截为"ecological-"但非空白）
  - 数量/完整名走悬停 tooltip
- **验证(Node 实跑真实 d3 布局)**：微生物与生态 3 格全带标签(metagenomics/microbiome 完整、ecological-genomics 竖排截断)；全局 21 完整 / 37 截断 / 6 无名(单-skill 极小格物理放不下，悬停+侧栏覆盖)；JS 语法 OK；server 8123 服务新版(HTTP200)
- **结论**：Treemap 优化后满足"按领域全景 + 小领域也看得见"，catalog 方案作废
- 预览：**http://localhost:8123**

## ✅ Treemap 改为「领域分行布局」+ 点击弹窗（最新一轮）
- **用户诉求**：① 调整布局，微生物与生态仍"展示不全"；② 增强交互，Treemap 格子点击弹出更详细信息的弹窗
- **布局改为「领域分行」**（彻底解决小领域被压扁）：
  - 每个**领域占一行**，行高 = `max(34, 域skill总量/总skill × 可用高)`（行高即体量感，最小 34px 保证可读）
  - 行内满宽按各 category 的 skill 数比例分块（块宽=占比）
  - 域头文字（中文领域名 + `· N`）恒显示在每行左上，永远可见 → 微生物与生态行完整显示"微生物与生态 · 20" + 3 个 category 格子全部带标签
- **点击弹窗（modal）交互**：
  - 点击任意色块 → `openModal(c,d)` 弹窗：域头(配色点+category名+×关闭) / 元信息(领域·skill数·已试用标记) / 若该 category=DONE(alignment)则含 004/005 深度篇链接 / 完整列出该 category 全部 skill（pairwise-alignment & msa-statistics 高亮 .sk-hl）
  - 关闭：点遮罩 / 点 × / 按 Esc
  - 新增 CSS：`.modal/.modal.open/.modal-card/.modal-hd/.modal-title/.modal-x/.modal-bd/.modal-meta/.modal-links/.mlink/.modal-sklabel/.modal-sk/.sk/.sk-hl` + `@keyframes modalIn`
- **修复关键 bug（Node 实跑发现）**：域对象本身无 `value` 字段（value 在子 category 上），原 `total=sum(d.value)` 与 `d.value/total` 全算成 **NaN** → 整张 Treemap 坐标 NaN 渲染空白。已在 TREEMAP_JS 开头加 `TREE.children.forEach(d=> d.value = sum(children))` 修正
- **标签覆盖再优化**：横排阈值 `w>54&&h>20`（57 格）+ 瘦高格 `(h>34&&w>=16)` 竖排（6 格）→ **63/64 带标签**，仅 `clawhub-installer`(w=12 极窄 sliver)无名，但域头+悬停+弹窗均覆盖
- **验证**：`node --check` 语法 OK；Node 实跑真实布局数学（10 域行高 34~119px、微生物与生态 rowH=34 三格全标签、63/64 覆盖）；本地 server 8123 服务新版(HTTP200)；style.css 含 modal 规则 ✅

## ✅ 回退：恢复「方形挤压 Treemap」（用户要求）
- **用户诉求**："我还是觉得上个版本好 回退一下吧" → 指回上一版（方形挤压 d3 Treemap + 领域着色 + 悬停明细 + 优化标签，**无分行、无弹窗**）
- **动作**：改 `build_lab_site.py` 把 TREEMAP_JS 从「领域分行 + 点击弹窗」改回 d3.treemap 方形挤压布局；删 map_sec 弹窗容器；删 STYLE 中 modal CSS 块
  - 布局：`d3.hierarchy(TREE).sum(d=>d.value||0).sort(...)` + `d3.treemap().size([920,600]).paddingOuter(2).paddingTop(20).paddingInner(3)`；域头写在每域顶部留白带（中文域名 + `· N`）
  - 标签：横排 `w>46&&h>24`（自适应字号+截断）+ 瘦高 `h>1.5*w&&h>=22` 竖排；悬停 tooltip（域/×N/已试用）；alignment 红框可点击跳 alignment/index.html
- **关键 bug（实跑 d3 发现并修复）**：原 TREEMAP_JS 开头 `TREE.children.forEach(d=> d.value=d.children.reduce(...))` 预先把域总量写进 raw data，之后 `d3.sum()` 把「域自身 value + 子节点和」相加 → 域头数字**翻倍**（微生物与生态 显示 40 而非 20，所有域 ×2）；叶子面积因分子分母同翻倍碰巧正确但域头文字错。已删预置行，改由 d3.sum 算出的 node.value（= 正确域总量）供域头
- **验证（Node 实跑真实 d3.treemap）**：10 域头 = 真实总量（109/99/61/53/50/45/44/42/39/20，合计 562）；64 叶 0 NaN；**64/64 全部带标签**（60 横排+4 竖排，比原版 58 更好）；微生物与生态 3 类全带标签；`node --check` JS 语法 OK；server 8123 服务新版(HTTP200)；HTML/CSS 中 modal 引用已清空
- 预览：**http://localhost:8123**

## ✅ 侧栏目录展开状态跨页面持久化（用户反馈）
- **问题**：多文件站点每点链接整页刷新，侧栏重新生成，只展开"当前页所在分支"，用户手动展开的其他层级全被收掉，体验差
- **修复（`build_lab_site.py` + 公共 `COPY_JS`）**：
  - 给三类 `<details>` 加稳定 `data-key`（域 `D:<域名>`、类 `C:<类名>`），供跨页面匹配
  - `COPY_JS` 新增 IIFE：读 `localStorage['bioLabNavState']`(key→bool)，页面加载时恢复每个 details 的 open/closed；含当前页(`.cur`)的层级**强制展开**（始终能看到自己在哪）；监听 `toggle` 事件实时保存手动展开/收起
  - 移动端抽屉关闭逻辑（`.sidenav a` 点击 `sd.classList.remove('open')`）保留，不与 details 状态冲突
- **验证**：首页 `data-key` 共 11 个（10 域 + alignment 的 C:）；`node --check` COPY_JS 语法 OK；server 8123 服务新版(HTTP200)。注：localStorage 行为需浏览器实测，逻辑已就位
- 预览：**http://localhost:8123**

## ✅ UI 细化：图片放大 + 图标复制 + 配色优化（用户最新请求）
- **用户诉求**：①html 已有图片点击可放大 ②表格和代码都用「图标」复制按钮（非文字"复制"）③页面配色优化；主体结构不动（只增强交互/图标/配色）
- **① 图片 lightbox**：STYLE 新增 `.lb{position:fixed;inset:0;z-index:200;background:rgba(15,23,42,.82);…}` 遮罩；`figure img` 加 `cursor:zoom-in` + hover 提亮；`COPY_JS` 注入 `.lb` 容器 + 监听 `figure img` 点击塞 src、点遮罩/`Escape` 关闭
- **② SVG 图标复制按钮**：`COPY_JS` 重写 `mkCopy()`(30×30 圆角半透明深底) + `bindCopy()`(复制图标→对勾图标+绿底`.ok`，1.4s 复位)；代码块 `pre code` 各挂一按钮(`block.textContent`)；表格自动 `.tbl-wrap` 包裹 + TSV 复制按钮(`tr>td` innerText 用 `\t` 连接)；触屏 `@media(hover:none)` 常显
- **③ 配色优化**：token `--accent:#6d5efc`(靛紫)/`--accent-d:#5338e0`/`--accent-soft:#efecfe`；侧栏 `.side` 改 `linear-gradient(168deg,#111b2c…)` + 靛紫描边 `rgba(109,94,252,.22)`；表格头 accent-soft 底 + accent-d 字
- **验证**：`node --check` COPY_JS 语法 OK；served style.css grep 命中 5/5(`--accent:#6d5efc`/渐变/`cursor:zoom-in`/`.lb{`/`.tbl-wrap{position:relative`)；alignment/pairwise/msa 全 200；pairwise 页含 1 `<pre`+1 `<figure`(被增强)。注：lightbox/复制/触屏常显需浏览器实测
- 预览：**http://localhost:8123**

## ✅ 架构重构：改成 md 驱动（SKILL.md 作单一事实源）
- **用户质疑**：网页内容应和笔记一致 + 怀疑在浪费 token；实际旧版只有"结构/代码块"自动(扫 `库/bioSkills` 文件夹 + `code()` 读实验 .py)，**讲解 prose 是手写死在脚本里的 Python 字符串** → 既费 token 又和 `SKILL.md` 脱节
- **决策（用户拍板）**：改成 md 驱动 + 渲染完整 SKILL.md
- **做法**：写模式驱动重构脚本(非手工 Edit，避免长段 unicode/转义匹配出错)：
  - 新增 `md_to_html(md_path)`：`open` 读 `库/bioSkills/<cat>/<skill>/SKILL.md` → 剥 YAML frontmatter(`re.sub(r"^---.*?\n---\n",…)`) → `_md.markdown(extensions=["tables","fenced_code"])` → 把 `<pre>` 包成 `<div class="codewrap"><pre>` 以匹配 COPY_JS 的 hover 复制按钮
  - `body004/body005` 由 `md_to_html(...)` 生成；删掉原 8+ 行硬编码 prose
  - `skill_page` 在代码附录前补 `<h2>本实验室真实复现代码</h2>`；附录仍读实验 .py(`code()`)保证"真实复现"价值
  - 删掉已无引用的 3 张实验 PNG 拷贝循环(死资源)
  - 依赖：`markdown` 库(managed venv 已 `pip install markdown` 3.10.3；用户 anaconda 自带)
- **验证**：`py_compile` OK；站点重生(pairwise 46KB/msa 48KB=完整 SKILL.md+附录)；served 核验：三页 200；旧 prose 命中 0；SKILL.md 内容命中 2；`<table>`/`.codewrap`/`language-python` 共 26；frontmatter 未泄漏(0)；index Treemap 完好(`id="tm"`)
- **取舍提醒**：原"真实试用"两张实验图(坑一 gap/坑二 PID)与"实测结论"框不在 SKILL.md 内 → 已从这两页移除；如要保留，建议挪到独立 trial-notes md 再一起渲染。扩展全部 562 skill 出页 = 在渲染列表加 slug 即可(一行)
- 预览：**http://localhost:8123**

## ✅ 纠正：单一事实源应是「笔记」而非 SKILL.md
- **用户纠正**：`网页≡笔记` 的"笔记" = `笔记/004-bioSkills真实试用-pairwise-alignment.md` 这份**真实试用文章**，不是 `库/bioSkills` 的 `SKILL.md`(上一轮接错源)
- **笔记结构**：顶部 `<!-- META ... /META -->` 注释块(标题/副标题/系列/配图/参考仓库/发布顺序) + 标准 md(标题/表格/代码块/`![](../素材/004-pairwise/xxx.png)` 相对图)
- **改 `load_note()` 取代 `md_to_html()`**：① 剥 `<!-- META -->` 注释块并解析 标题/副标题 喂 masthead(替代硬编码) ② YAML `---` frontmatter 作兜底 ③ 图片 `![](rel)` 改写 `../assets/<basename>` 并收集源路径 ④ markdown(tables+fenced_code) + `<pre>` 包 `.codewrap`
- **写出段**在 `os.makedirs(ASSETS)` 后按收集源图拷进 `assets/`(避开 rmtree 先删后建顺序坑)；`skill_page` 附录 `code_blocks=[]`(笔记自身已含复现结果与图，不再单独注入实验 .py)
- **验证**：两页 200；笔记内容命中 2；实验图回归(004 两张+005 一张，均已拷 assets)；旧 SKILL.md 内容 0；META 注释 0；h1 取自笔记 META(`真实试用 bioSkills pairwise-alignment：默认 gap=0...`)；Treemap 完好
- 预览：**http://localhost:8123**

## ✅ Bug 修复：笔记图片无法点击放大 + 失 frame 样式
- **现象**：切到笔记源后，图片点不放大、且"css 效果没了"
- **根因**：笔记渲染出裸 `<img>`(在 `<p>` 内)，而 lightbox JS 只绑 `figure img`、zoom 光标/边框样式也只给 `figure img` → 匹配不到
- **修复(3 处)**：① `load_note()` 渲染后 `re.sub(r"<p>\s*(<img[^>]*>)\s*</p>", r"<figure>\1</figure>")` 把独立图包进 `<figure>`(恢复边框+让 figure 选择器生效) ② COPY_JS `querySelectorAll('figure img')`→`'.content img'`(所有正文图可放大，防御性) ③ STYLE `figure img{...}`→`figure img, .content img{...}`(zoom 光标扩到全部正文图)
- **验证**：两图已包 `<figure>`；lightbox 绑 `.content img`；style.css 含 `figure img, .content img`；HTTP 200
- 预览：**http://localhost:8123**

## ✅ 工作流审计 + 落地两个受控 skill（S3 受控化 + S1→S5 编排）
- **用户审计诉求**："从 skills 到试用产生 md 转 html 关联到小红书站点 是否是一个完整的工作流了 有没有每个阶段的 skills 控制"
- **S0–S5 受控性结论**：S0(源,只读)/S2b(`redbook-bio-note-writer`)/S4(`dingcard-layout-export`) 已受控；S3(转 HTML，裸脚本 `build_lab_site.py`) / S2a(真跑复现) / S5(互链闭环) 不受控
- **用户授权**："嗯嗯 都要 你来定" → 落地两条建议
- **① 新建 `bioSkills-lab-site`**（`~/.workbuddy/skills/bioSkills-lab-site/SKILL.md`，治理 S3）：固化 `实验/build_lab_site.py` 契约——
  - 事实源 = `笔记/*.md`(单一事实源，非 SKILL.md)；META 注释块契约(与 S2b/S4 共用，发布前剥离)；`load_note()` 语义(剥META/解析标题副标题/改写+拷贝 `../素材/` 图/fenced 包 `.codewrap`/裸 `<img>` 包 `<figure>`)
  - 输出结构(`产出/bioSkills-site/`)+ 执行方式(managed venv 跑 `markdown` 3.10.3)+ 本地预览(8123)+ 校验清单 + 老化维护边界(**作用域纪律：不改动已跑通脚本逻辑**，改须先在 /tmp 备份并回填契约变更)
- **② 新建 `bioSkills-trial-pipeline`**（`~/.workbuddy/skills/bioSkills-trial-pipeline/SKILL.md`，编排 S1→S5）：总编排串起各阶段受控 skill
  - S2a **真跑复现 Gate**(写笔记前必过，禁止虚构)：真实运行代码/真实数据图/与原方案一致/成分拆解/坑点记录 五条全满足才算过，未过则停 S2a 报告用户
  - 关键路径速查(库/bioSkills→实验→素材→笔记→产出→小红书)+ 三端 META 字段一致性约束 + 诚信红线说明
- **验证**：两 SKILL.md frontmatter OK(name 正确、description 完整)；`build_lab_site.py` `py_compile` 通过(未改动)；用户 skills 仓库未自动 git 提交(须用户手动 `git add`+commit+push 到 Diandian-ops/workbuddy-skills)
- **当前受控闭环**：S1(人工选题) → S2a(Gate) → S2b(`redbook-bio-note-writer`) → S3(`bioSkills-lab-site`) → S4(`dingcard-layout-export`) → S5(互链闭环)，全链路均有 skill 控制

## ✅ 受控性现状确认 + 后续优化约定（用户追问）
- **用户追问**："是不是每一个阶段都有 skills 掌控？后续做同类内容时，若对应 skill 有优化你再提就好，是不是？"
- **诚实结论（非全"独立 skill"）**：
  - 全控(独立 skill)：S2b `redbook-bio-note-writer` / S3 `bioSkills-lab-site` / S4 `dingcard-layout-export`
  - 半受控(编排 skill 内管，无独立 skill)：S2a 真跑复现(Gate 清单管纪律，但"怎么复现"执行知识未封装) / S5 互链闭环(步骤描述)
  - 刻意不放：S0 源仓库(外部只读) / S1 选题(人工决策)
- **已知缺口（已主动提出，未擅自建）**：S2a 缺独立 reproduce skill；判断暂不需补（每 skill 复现方法异质，抽象价值有限），除非日后批量 trial 且套路可沉淀
- **工作约定（已与用户确认）**：后续做同类内容时，若发现对应 skill 有可优化点（契约漂移/踩坑/流程可顺），助手**主动提出但不自动改**，由用户拍板

## ✅ 004/005 笔记头部去重（S3 站点双 h1 修复）
- **用户发现**：笔记"头部分"有内容重复、不合理 → 经查渲染产物：pairwise/msa 两页**都有 2 个 `<h1>`**（masthead 的 META 标题 + 笔记正文自带 `# H1`）+ 正文首段 lead 重复 META 副标题
- **根因**：笔记是单一事实源，被两个渲染器消费——S4 DingCard(剥 META，靠正文 H1+lead 当标题块，必须留) / S3 站点(masthead 已渲染 标题+副标题，正文 H1+lead 叠成重复)。笔记本身没错，问题在 S3 生成器未去重
- **修复（`实验/build_lab_site.py` 的 `load_note()`）**：META 含 `标题` 时，抑制正文开头 `<h1>` + 紧跟 `<p>` 引导段(+随后的 `<hr>`)；无 `标题` 保留正文 H1 兜底。DingCard 读原始 md 不受影响
- **踩坑**：首版正则 `<p>[^<]*</p>` 在 lead 含 `**加粗**`/`` `code` ``（渲染出 `<strong>`/`<code>`）时不匹配 → msa 页去重失效；改为 `.*?` 兼容内嵌标签后两页均只剩 1 个 h1
- **验证**：pairwise/msa 各 1 个 `<h1>`(=META 标题)；5 个 `<hr />` 分隔线 + 7 个 `<table>` 均在；正文从 `## 这个 skill 是什么` 干净起头；HTTP 200(根=产出/bioSkills-site，路径 `/alignment/...`)
- **Skill 同步**：`bioSkills-lab-site/SKILL.md` 的 load_note 契约新增第 6 条(去重规则)，契约与实现一致
- **用户批准方式**：按"先提不自动改"约定提出 → 用户"按你建议来"授权 → 已落地

## ✅ 新建 bioSkills-figure-quality skill + 三张图实际修复（S2a 图质量 Gate）
- **用户发现**：bioSkills 试用产生的图没有 skill 管控效果——legend 压字、标注重叠、配色语义混乱
- **实际诊断（逐图看）**：
  - 004-fig-gappitrap.png：红色标注 `"default adds 46 spurious gaps"` 与右上角 legend 框**重叠** 🔴
  - 004-fig-pid.png：底部灰色说明与 x 轴 PID1–PID4 标签**重叠** 🔴
  - 005-fig.png 面板 A：`"max H=log2(20)=4.32"` 自由浮文本压在熵数据线上 🟡；面板 B 配色语义反了（correct=红, WRONG=灰）
- **新建 `bioSkills-figure-quality`**（`~/.workbuddy/skills/bioSkills-figure-quality/SKILL.md` + `fig_quality.py` helper）：管控 S2a 出图质量——7 条硬规则(legend 安全区/文字不重叠/dpi≥150/字体≥8pt/语义配色) + 可复用 helper(`save_clean`/`place_legend`/`caption`/4 色常量)
- **三图修复实战**（内联改既有脚本，保持可复现性）：
  - 004 图1：legend→plot 外右外(`bbox_to_anchor=(1.01,1.0)`)；标注从 plot 内 y=0.92 改为**图底部 caption**(`fig.text(0.5,0.02,...)`+`subplots_adjust(bottom=0.16)`)——经历两次迭代(y=0.92 撞柱顶172 → y=1.06 撞标题 → 最终底部 caption 干净)
  - 004 图2：底部说明从 `ax.text(0.5,0.04,...)`(压 x 轴)改为 `fig.text(0.5,0.02,...)`(轴标签之下)
  - 005 面板 A：自由浮文本→`axhline` 的 `label` 进 legend；面板 B：uniform→红(WRONG) / Robinson→绿(correct)；dpi 130→150
- **踩坑沉淀到 skill 契约**：新增"严禁 y>0.85 放标注（会压标题/高柱）"；关键结论统一走图底部 caption 模式
- **验证**：三图肉眼确认无重叠/无压字；站点重生成(HTTP 200)；skill frontmatter OK

## ✅ 小红书精简版（封面卡 + 实测结果）+ 模板固化
- **用户需求**：小红书引流需要精简版文档，结构=1 张功能简介图 + 后面实测结果；做成模板以后复用
- **用户拍板**：封面卡用"生成品牌标题卡"(matplotlib on-brand)；范围=做成模板(以后复用)
- **新建 `实验/make_cover.py`**（可复用封面卡生成器）：portrait 3:4 (1080x1440)，编辑档案风 #eee8db + 红强调 #b5341f，CLI 参数(--out/--name/--line/--cap/--accent)，PingFang SC 中文
- **生成两张封面卡**：
  - `素材/004-pairwise/004-cover.png`(92KB)：pairwise-alignment / 两条序列最优比对 / BLOSUM62+gap+PID
  - `素材/005-msa-statistics/005-cover.png`(88KB)：msa-statistics / 多序列比对定量指标 / 熵+IC+替换计数
  - 两张均目视通过（红条标签→大名→功能→分隔线→核心能力3点→脚注，无重叠）
- **写两篇精简小红书笔记**（`笔记/00X-bioSkills小红书-*.md`）：
  - 004：META + 封面卡 + "我拿真实数据跑通了" + 坑一(gap=0 陷阱+gappitrap 图) + 坑二(PID 四口径+pid 图) + 一句话结论
  - 005：META + 封面卡 + "我拿真实数据跑通了" + 核心坑(uniform vs Robinson IC+双面板图) + 一句话结论
  - 结构=封面卡(1) + 实测结果(2-3 卡) = 总 3-6 卡，符合用户"精简"要求
- **DingCard 导出两篇 ZIP**（编辑档案模板）：
  - `产出/004-bioSkills小红书-pairwise-alignment-editorial.zip`(1.3 MB)
  - `产出/005-bioSkills小红书-msa-statistics-editorial.zip`(1.3 MB)
- **模板固化到 `redbook-bio-note-writer/SKILL.md`**：新增"八、小红书精简模板（引流版）"——含 markdown 模板/make_cover.py 用法/与完整版对比表/figure-quality Gate 要求/DingCard 导出命令
- **完整链路现在有两条输出路径**：(A) 完整试用笔记→S3 站点+S4 小红书 (B) 精简版笔记→S4 小红书引流

## ✅ 小红书精简版补"功能介绍+解决问题"开头（用户反馈）
- **用户反馈**："小红书版本至少应该介绍一下 skills 的功能、预期解决问题，不然读者看到就蒙圈了"
- **根因**：封面卡只做视觉简介，正文直接跳"我跑通了+坑点"，缺"这 skill 是干嘛的/解决啥问题"铺垫
- **修复**：两篇精简笔记在封面卡后新增"这个 skill 是干嘛的"段——功能定义 + **解决啥问题**(使用场景) + 我拿真实数据测了(承接复现)
  - 004：两条序列动态规划最优比对 / 同源序列差多少·哪保守 / Biopython PairwiseAligner 常用但参数坑多
  - 005：从 MSA 提取定量指标(熵/IC/替换) / MSA 肉眼看不出门道需量化 / 进化分析·保守位点鉴定用
- **重导出**：两 ZIP 更新(各 1.6 MB)，卡片数 004=5 / 005=4，仍在 3-6 卡区间
- **待固化**：此"功能介绍+解决问题"开头应写进 redbook-bio-note-writer 的精简模板(本次未改 SKILL.md，下次一并更新)

## ✅ DingCard 废弃决策（用户拍板）
- **用户决策**："有笔记就可以了，产出这里小红书的压缩包没有实际意义，后续不要产生了，dingcard 这个实际使用效果不好"
- **立即执行**：删掉本会话生成的 4 个 DingCard ZIP（`产出/004-bioSkills小红书-pairwise-alignment-editorial.zip`、`产出/005-bioSkills小红书-msa-statistics-editorial.zip`、`产出/004-bioSkills真实试用-pairwise-alignment-editorial.zip`、`产出/005-bioSkills真实试用-msa-statistics-editorial.zip`）
- **更早会话产物（001–003，8 个 ZIP）未动**：`001-生信AI-发布版.zip / 001-生信AI-排版.zip / 002-bioSkills-发布版.zip / 002-bioSkills-排版.zip / 002-bioSkills使用体验-editorial.zip / 003-bioSkills-alignment-发布版.zip / 003-bioSkills-alignment-排版.zip / 003-bioSkills拆解01-alignment专题-editorial.zip` —— 已列出请用户确认是否一并清理，等拍板
- **Skill 文档改造（已落地）**：
  - `redbook-bio-note-writer/SKILL.md`：① 顶部声明"md 笔记即最终交付物，DingCard 导出已停用" ② 第七节完整链路改为"md→bioSkills-lab-site 站点"为主链、DingCard 标⚠停用 ③ 第八节精简模板**固化"这个 skill 是干嘛的（功能+解决啥问题）"开头段** ④ 第八节导出流程标⚠已停用（仅留命令作历史参考，不执行）
  - `dingcard-layout-export/SKILL.md`：顶部加"⚠ 已停用（仅供历史参考）"横幅
  - `bioSkills-figure-quality/SKILL.md`：出图去向说明从"进笔记(DingCard 小红书)+站点"改写为"进笔记+站点(bioSkills-lab-site 渲染)"
- **结论**：S2b 笔记 + S3 站点 = 主交付链；S4 DingCard 退出。`bioSkills-trial-pipeline` 编排里的 S4 阶段描述仍提 DingCard，待用户在 S4 节点确认是否同步更新（按"先提不自动改"约定，已主动提、未擅改）

## ✅ 清理产出 DingCard 全部遗留（用户授权）
- **用户授权**："需要的 清理掉 保持工作环境的纯净" → 把 产出/ 内所有 DingCard 遗留产物清空
- **删除清单（共 18 项）**：
  - 8 个 ZIP：001-生信AI-发布版/排版、002-bioSkills-发布版/排版/使用体验-editorial、003-bioSkills-alignment-发布版/排版/拆解01-alignment专题-editorial
  - 1 个目录 `001-生信AI-发布版/`（内含 8 张 card-*.png，发布版 ZIP 解压产物）
  - 9 张 preview/debug PNG：001/002/003 的 -preview.png、003-final-state.png、003-injected-preview.png、004/005 的 小红书/真实试用 -preview.png
- **保留**：`bioSkills-site/`（真实交付物，md→站点主链产物）、`.DS_Store`
- **清理后 产出/ 顶层**：仅 `bioSkills-site/` + `.DS_Store`，环境纯净
- **待确认**：`bioSkills-trial-pipeline/SKILL.md` 的 S4 阶段描述仍写「→ S4 dingcard-layout-export」，与已废弃现状不符；按"先提不自动改"约定，待用户拍板是否同步改为主链描述

## ✅ 项目目录净化（用户授权"按建议来"）
- **背景**：评估 `草稿/策略/实验/素材` 是否还需。经 grep 核实依赖：`实验/build_lab_site.py` 硬依赖 `库/bioSkills`(类别地图)、`素材/004-pairwise`、`素材/005-msa-statistics`、`笔记/004`、`笔记/005`；早期 AI 插画(`001-生信AI/草稿-fastp/废弃/002 的 Flat_*`)零引用
- **已删（8 项零功能依赖）**：
  - `素材/001-生信AI/`、`素材/草稿-fastp/`、`素材/废弃/`
  - `素材/002-bioSkills/Flat_realistic_cover_image_for_2026-08-24T13-13-11.png`（保留 `bioSkills-cover.png`）
  - `草稿/`（整目录，fastp 未发布草稿）
  - `策略/`（整目录，账号定位+合集简介 规划文档）
  - `实验/__pycache__/`、`实验/build_lab_html.py`（死代码，已被 build_lab_site.py 替代）
- **保留**：`实验/`(生成器+复现脚本)、`素材/004-pairwise`、`素材/005-msa-statistics`、`素材/003-alignment`、`素材/002-bioSkills/bioSkills-cover.png`、`库/`、`笔记/`、`产出/bioSkills-site/`、`README.md`
- **验证**：`实验/` 四件核心 + `素材/003-alignment/` 原样；顶层 草稿/策略 消失，库/笔记/实验/素材/产出 在
- **仍待定**：`素材/003-alignment/` + `笔记/003-bioSkills拆解01-alignment专题*.md` 是否一并清——取决于 003 笔记去留，用户未拍板，暂留（003 不在当前站点渲染）

## ✅ 目录改名 实验 → pipeline（架构分层修正）
- **触发**：用户质疑"实验能和最后网站关联一起吗 是不是不太合适"。架构评估结论：构建源→生成器→产物(`产出/bioSkills-site/`)的关联本身正确且必须；真正"不太合适"的是 `实验/` 命名错位——它既装生产构建工具(`build_lab_site.py`/`make_cover.py`)又装真实复现实验(`004/005`)，名字叫"实验"却住着正式构建脚本。
- **决策**：用户选"重命名 实验→pipeline"（最小对症，不拆 trials）。
- **执行**：`mv 实验 pipeline`；同步更新 13 处引用——`pipeline/build_lab_site.py`(EXP004/EXP005 路径+注释+两处网页 UI 文案"实验/ 目录"→"pipeline/ 目录")；`bioSkills-lab-site/SKILL.md`(5处路径)；`bioSkills-trial-pipeline/SKILL.md`(2处 `实验/<NNN>`→`pipeline/<NNN>`)；`redbook-bio-note-writer/SKILL.md`(2处 make_cover.py 路径)。记忆日志历史行保持 append-only 不改写。
- **验证**：活代码/skill 中 `实验/` 路径引用清零(仅记忆日志留历史)；`py_compile` 通过；重新生成站点成功，`index.html` 页脚已渲染 `pipeline/ 目录复现`，产物无 `实验/ 目录` 残留。
- **仍待定(独立项, 未动)**：`bioSkills-trial-pipeline/SKILL.md` 的 S4 阶段仍写 `dingcard-layout-export`（DingCard 已停用），与主链不符——按"先提不自动改"约定，之前已提、用户尚未拍板是否改。

## ✅ 源归类进 content/（源/产物分层收口）
- **用户质疑**："素材和笔记都是否放到产出中更合适呢？" —— 架构评估结论：**方向反了**。"产出"是纯产物(`产出/bioSkills-site/`)，`build_lab_site.py` 重生成时先 `rmtree(SITE)` 再建，若源进产出会被误删(rmtree 雷区)；正确做法是把源收进 `content/`，与 `pipeline/`(构建脚本)、`产出/`(产物) 三层清晰分离。
- **决策**：用户选"源归类进 content/"。
- **执行**：`mkdir -p content && mv 笔记 素材 库 content/`；顶层收敛为 `content / pipeline / 产出` + README。
- **引用同步(全量)**：
  - `pipeline/build_lab_site.py`：5 处硬路径加 `content/` 前缀（BIO/IMG004/IMG005/NOTE004/NOTE005）。
  - 负向后顾正则批量替换（`re.sub(r'(?<!\.\./)笔记/', 'content/笔记/', t)` 同类）4 个主 skill 文档 + `dingcard-layout-export/SKILL.md` + `pipeline/004-pairwise-redo/make_figs.py` + `pipeline/make_cover.py`，根级 `笔记/素材/库` 收敛为 `content/` 前缀；`../素材/` 相对链接**保留不动**（笔记与素材作为兄弟目录同迁 content/，相对关系不变 → 笔记 .md 零改动）。
- **验证**：① `py_compile` 三脚本通过；② 端到端重生成成功——`产出/bioSkills-site/`(index 38KB/style 9KB/alignment 15KB/msa 24KB/pairwise 25KB + assets 三图 004-fig-gappitrap/004-fig-pid/005-fig 均正确拷入)；③ grep 确认活代码/skill 中根级源路径已清零（相对链接除外）。
- **README 重写**：目录树改为 content/pipeline/产出 三层 + 源产物分层警示；笔记索引链接改 `content/笔记/`；删除已不存在的 `策略/草稿/素材/废弃/` 描述；新增"交付链路受控闭环"段（含 DingCard 停用说明）与"如何新增一篇"的 content/ 路径。记忆日志 append-only 不改写历史。

## ✅ 目录治理全收尾 + 当前状态（本轮收口）
- **当前顶层结构**：`content/`(源: 库/笔记/素材) · `pipeline/`(构建脚本 build_lab_site.py/make_cover.py + 004/005 复现实验) · `产出/`(纯产物 bioSkills-site/) · `README.md`；`.venv`/`.env-mafft`(复现环境隔离) · `.workbuddy/`(记忆)。RedBook 非 git 仓库。
- **主交付链**：S2b 笔记(md) + S3 站点(bioSkills-site) = 当前唯一对外载体；DingCard(S4) 已停用，不再产 ZIP。
- **4 个 user-level skill**（~/.workbuddy/skills/）：`redbook-bio-note-writer`(S2b 受控) · `bioSkills-lab-site`(S3 受控) · `bioSkills-trial-pipeline`(S1→S5 编排) · `bioSkills-figure-quality`(S2a 图质量 Gate)；`dingcard-layout-export` 标⚠停用。
- **唯一仍待拍板（独立尾巴）**：`bioSkills-trial-pipeline/SKILL.md` 的 S4 仍写 `dingcard-layout-export`（第 20/30/56/59 行），与已停用现状不符——已提、按"先提不自动改"约定待用户点头是否同步改为主链描述。

## ✅ DingCard 尾巴收口（用户授权"改 + 库中的 dingcard 清理"）
- **用户授权两项**：① 改 `bioSkills-trial-pipeline/SKILL.md` 的 S4 DingCard 引用 → 标"已停用"；② `content/库/DingCard/` 也可以清理（删除）。
- **删除前尽职调查**：grep 全仓 DingCard 引用——`pipeline/build_lab_site.py` 仅 47–48 行**注释**提及（解释 H1 去重设计由来），**非硬路径依赖**；`content/笔记/003-..._dingcard.md` 为历史笔记；`content/库/DingCard/` 为 114MB 外部 git clone（lottshin/DingCard，可 `git clone` 重建）。结论：删目录不影响站点构建。
- **执行**：
  - `bioSkills-trial-pipeline/SKILL.md` 4 处 S4 改写（表格行 + 编排 step5 + note-writer 要点 + S4 调用要点）→ 统一标"已停用"，不再声称调 `dingcard-layout-export` 出 ZIP。
  - `rm -rf content/库/DingCard/`（114MB 外部 clone，已授权）。
  - README 目录树删 `库/DingCard/` 那一行（保留 obsidian-wechat-converter）。
- **验证**：删后重跑 `python pipeline/build_lab_site.py` 成功（`产出/bioSkills-site/` 完整重建 + 三图正常拷入）→ 证明零硬依赖；`content/库/` 现仅 `bioSkills` + `obsidian-wechat-converter`。
- **遗留（已知、未动、功能性惰性）**：`dingcard-layout-export/SKILL.md`（已标⚠停用）第 21/24/25/138 行仍指向已删的 `content/库/DingCard/...` 路径，现为悬空引用；因该 skill 已停用仅供历史参考，不影响任何活链路，按范围纪律未擅改，仅标记待用户决定。

## ✅ git 初始化 + 产出→output 改名（资产保护与 ASCII 化）
- **用户授权**：① `git init` 给 `content/` 上备份；② 把 `产出/` 改成英文（用户问"你觉得呢"，我荐 `output/`——最直接无歧义、且规避中文目录在构建/托管/本地路径引用时的编码/URL 转义坑；`dist`/`site` 为备选）。
- **执行**：
  - `git init`（RedBook 此前非 git 仓库）；复用全局身份 `ShengXinF3 <shengxinf3@163.com>`。
  - `mv 产出 output`；全量替换 `产出/`→`output/`（负向后顾式：仅替换带斜杠的**目录路径**，笔记/HTML 内的中文"产出"词不动）：`build_lab_site.py`(1) / `README.md`(7) / `bioSkills-lab-site`(6) / `bioSkills-trial-pipeline`(4) / `redbook-bio-note-writer`(3) / `bioSkills-figure-quality`(1) / `dingcard-layout-export`(1)。
  - `.gitignore`：忽略 `output/`(可重建站点) + `.venv/` + `.env-mafft/` + `__pycache__/` + `node_modules/` + `.DS_Store`。
  - 首版提交：`content/`(笔记/素材) + `pipeline/` + `README.md` + `.gitignore`（共 38 文件；`output/` 因 gitignore 未跟踪）。
- **验证中发现的真问题（已修）**：`content/库/bioSkills` 与 `content/库/obsidian-wechat-converter` 是外部 git clone（自带 `.git`），首次 `git add` 被当作 **gitlink 嵌入仓库**——只存指针不存内容，RedBook 新克隆会得空目录、构建必崩。修复：`git rm --cached` 两个 + 写进 `.gitignore`（各自有上游 GPTomics/bioSkills、obsidian-wechat-converter，不纳入本库备份；本库只备份自身源，构建前按需 `git clone` 上游）。验证：两 clone 磁盘与 `.git` 完好、被正确忽略、工作树干净。
- **最终验证**：删后重跑 `python pipeline/build_lab_site.py` 成功，产物落在 `output/bioSkills-site/`（index 38KB/style 9KB/alignment 15KB/msa 24KB/pairwise 25KB + assets 三图），`output/` 仍被 git 忽略 → 改名 + 版本控制全链路通过。
- **待办（可选，未动）**：`RedBook/.workbuddy/`(项目每日工作日志 memory) 当前未跟踪（`git status` 显示 `?? .workbuddy/`）。要不要纳入版本库由用户定——它是有价值的项目记忆，但含工作内容日志，按需决定。
- **skills 仓库（~/.workbuddy/skills，独立 git 仓库 Diandian-ops/workbuddy-skills）**：见末尾「远程库 skills 同步」段——已执行 `git rm dingcard + add 4 主 skill + commit + push`，本地与远程对齐到 `7f3b130`。

## 收口：项目记忆纳入版本库 + skills 命令清单
- **RedBook 仓库**：用户拍板"按建议来" → 把 `.workbuddy/memory/`(2026-08-24.md / 2026-08-25.md) 纳入版本库（`git add .workbuddy/memory/ && commit`），工作树干净。项目记忆从此有版本备份。
- **skills 仓库同步（用户自理）**：未替用户 push。已核验 5 个被改文件均带 `output/`、零 `产出/` 残留：`bioSkills-lab-site`(5处)/`bioSkills-trial-pipeline`(4处)/`redbook-bio-note-writer`(3处)/`bioSkills-figure-quality`(1处)/`dingcard-layout-export`(1处，已停用)。其中 4 个主 skill 目录整体此前**从未提交(未跟踪)**，仅 `dingcard` 为已跟踪-M。已给用户精确 `git add <5个SKILL.md> + commit + push` 命令清单；并提示仓库另有 `.disable_to_model_invocation_migration.json` / `electron-renderer-input-debug/` / `ui-audit-check-compliance/` 等未跟踪项，非本次改动、提交前自行甄别。


## 卡片导出链路全清理（用户拍板"都清理掉"）
- 删除用户级 skill 目录 `~/.workbuddy/skills/dingcard-layout-export/`（已停用，无任何活链路依赖）。
- 清理 3 个相关 skill 中全部 DingCard/`dingcard-layout-export` 引用（`bioSkills-trial-pipeline` 6 处 / `bioSkills-lab-site` 6 处 / `redbook-bio-note-writer` 6 处含整段删除"导出流程"死代码）；S4 标注"不再自动导出"、META 剥离改由 S3/手动。
- 删除 `content/笔记/003-..._dingcard.md` 历史遗留笔记（未入站点构建，含 DingCard 拖图指令）。
- 清理 `pipeline/build_lab_site.py` 注释 L47-48（去掉 DingCard 归因，保留去重逻辑说明）+ README 目录树/S4 引用 + 旧更新记录中产品名。
- 验证：skills 仓库 + RedBook 源（除本 memory 历史日志）零 dingcard 残留；rmtree 雷区不受影响（源仍在 content/）。
- 注：skills 仓库已同步远端（见「远程库 skills 同步」段）——`git rm dingcard-layout-export + add 4 主 skill + commit + push` 已执行，远程 dingcard 移除、4 主 skill 首登远程且带 `output/` 路径，本地/远程均 `7f3b130`。

## 远程库 skills 同步（用户拍板"适配一下"）
- 用户授权把本地 skills 仓库改动同步到远程 Diandian-ops/workbuddy-skills。
- 先 `git fetch` 核查：本地 HEAD 与 origin/main 均为 `407a86b`，远程无新提交、本地不落后 → 双终端无冲突，可安全 push。
- 核查 4 个主 skill 目录内容纯净（仅 SKILL.md，其一含 fig_quality.py；无二进制/大文件），可整目录提交。
- 执行：`git rm --cached dingcard-layout-export/SKILL.md`（记录删除）+ `git add bioSkills-figure-quality/ bioSkills-lab-site/ bioSkills-trial-pipeline/ redbook-bio-note-writer/`（首次提交 4 主 skill）+ commit "chore: sync RedBook restructure — drop dingcard-layout-export, align 4 skills to output/ path" + `git push origin main`。
- 结果：`407a86b..7f3b130 main -> main`，本地/远程均 `7f3b130`。未跟踪项 `.disable_to_model_invocation_migration.json` / `electron-renderer-input-debug/` / `ui-audit-check-compliance/` 仍留未跟踪，未被误加。
- 至此：RedBook 目录治理全部改动（content/pipeline/output 三层 + dingcard 全清）已贯通到 skills 远程仓库，跨机同步一致。

## 006 选题 S1 评估（用户授权"从 alignment/ 里你选"）
- 用户拍板 006 选题授权模型自主选，方向限定 `content/库/bioSkills/alignment/`。
- alignment/ 下 7 skill：alignment-io / alignment-trimming / msa-parsing / msa-statistics(=005) / multiple-alignment / pairwise-alignment(=004) / structural-alignment。
- **S1 推荐：006 = alignment-trimming（比对修剪）**。理由：递进 004(pairwise)/005(msa) 的"比对"主线、填补后处理空白、强出图潜力（修剪前后列保留对比），契合"真实数据图"小红书风格。
- 可行性核查：① 修剪工具 clipkit/trimal/BMGE 均**未安装**（conda base 亦无）；biopython 1.86 在。② 005/004 素材**仅落 PNG、无原始 MSA 文件** → 006 不能复用既有数据，须自备真实 MSA 输入。
- 结论：主题可行、价值高；S2a 第一步须 装工具(clipkit，优先 managed venv pip 隔离) + 取真实 MSA 输入，再跑 kpic-smart-gap 出"修剪前后列保留对比图"。待用户拍板进 S2a。

## 006 定题 + 执行纪律（用户拍板"定了"）
- 006 = alignment-trimming（S1 评估已定）。
- **用户硬性约束**："skills要规定好了 完全复现 不要 自己加内容" → 006 及以后真实试用须严格按对应 SKILL.md 执行，不自行加参数/步骤/内容；已记入项目 MEMORY.md 长期约定。
- S2a 启动：clipkit 2.14.0 已装进 managed venv（`pip install clipkit`，隔离、不污染用户环境）。
- 真实 MSA 输入锁定：`content/库/bioSkills/alignment/alignment-io/examples/sample_alignment.aln`（bioSkills 仓库自带示例比对，非自造，符合"完全复现"）。

## 006 S2a/S2b/S3 完成（严格按 skill 规定、完全复现）
- S2a 真跑：clipkit 2.14.0（managed venv）对 bioSkills 自带示例 `alignment-io/examples/sample_alignment.aln`（CLUSTAL，4 序列 21 列）跑 `kpic-smart-gap --log`，结果 21→20 列、砍第 13 列（gap 0.75）、保留 95.2%。
- 出图：`pipeline/006-trimming/make_fig.py` → `content/素材/006-trimming/006-fig.png`（列级保留对比图，第 13 列标红），matplotlib 3.11.1（managed venv）。
- S2b 笔记：`content/笔记/006-bioSkills真实试用-alignment-trimming.md`（引流精简版，内容仅基于 SKILL.md 知识 + 真实跑结果，未夹带 trimal/BMGE 等未跑工具结论）。
- S3 站点：`build_lab_site.py` 加 006 注册（ALIGN_SUBS/sidebar 两处激活/line138 跳过/al 分支页 DONE 006/待做列表/NOTE006/load_note/skill_page/图片依赖/写出 共 10 处），重建成功 → `output/bioSkills-site/alignment/alignment-trimming.html`(16KB) + `assets/006-fig.png`(43KB)。
- 全程遵守用户纪律："skills要规定好了 完全复现 不要 自己加内容"（数据用 bioSkills 自带示例、命令照 SKILL.md、笔记不夹带）。

## 006 小红书版补漏（用户质疑"小红书版本没直接出来"）
- 006 S2b 初只产了 `真实试用` 版，漏了 `小红书` 版——与 004/005 的"两版齐全"惯例不符。
- 补 `content/笔记/006-bioSkills小红书-alignment-trimming.md`（META + 封面卡 + 这个skill是干嘛的 + 发现一(kpic-smart-gap 砍 gap 列,表+006-fig.png) + 发现二(--log 可复现) + 一句话结论；数字/命令原样搬运真实试用版，零夹带）。
- 补封面卡 `content/素材/006-trimming/006-cover.png`（82KB）：`pipeline/make_cover.py` 生成，对齐 004/005 做法（--name alignment-trimming / 两行功能 / 三点核心能力）。
- **纪律固化（流程教训）**：每个 trial 的 S2b 必须同时产出两版——`真实试用` md（进站点 S3）+ `小红书` md（发布稿，含封面卡）。不可只产其一。小红书版不进站点构建（仅真实试用版注册进 build_lab_site.py），封面图仅供发布稿引用。

## 006 真实试用改写为深拆版 + redbook-bio-note-writer skill 规范补全（用户："真实试用也要像005一样 skill成分拆解，只有小红书才精简，目前skill没这么规范吗"）
- **问题**：006 真实试用初稿被写成"引流精简风"（55 行），与 005 的 189 行深拆版（成分拆解+严格复现+坑实测）不一致；根因是 `redbook-bio-note-writer` 只在抽象表区分"完整试用笔记 vs 小红书精简版"，**缺深拆版具象模板，且未明说"每轮出两份文件"**——规范缺口。
- **修 006 真实试用**：重写为 005 风格深拆版（约 150 行）——这个skill是什么(属性表)+ Skill成分拆解(文件结构表/4个示例脚本/9种修剪方案工具知识/核心API片段/经验与知识:20%40%法则·trimAl-automated1不可审计·选择分析禁激进·列映射可复现)+ 严格复现(环境/数据来源=bioSkills自带示例/标准输出/发现一实测kpic-smart-gap砍gap列/发现二--log/关于未跑工具的诚实声明)+ 教对了什么 + 小结。严格复现只报告实际跑过的 clipkit kpic-smart-gap，其余 8 方案忠实转述 SKILL.md、不编造结果。
- **修 skill 规范**：`redbook-bio-note-writer/SKILL.md` 第八节重构为"两种笔记形态（每轮必须同时产出两份）"——加 8.1 真实试用深拆模板（具象 markdown，对齐 005）+ 8.2 小红书精简模板（原内容下移）+ 两文件纪律表（命名/形态/是否进站点）+ 改对比表表头"完整试用笔记"→"真实试用笔记（8.1）"。
- **长期约定固化**：项目 MEMORY.md 加"两份文件纪律"条款（真实试用=深拆进站点 / 小红书=精简仅发布稿；不可只产其一；规范已在 note-writer 第八节）。
- 重建站点验证：006 页面 16KB→26KB，含 成分拆解(3)/严格复现(2)/20%40%(3)/kpic-smart-gap(10)/关于未跑的工具(1)/这个skill教对了什么(1)，py_compile 通过、重建 EXIT=0。
- **状态收尾**：006 = alignment-trimming 全链路（S1→S3 + 两版齐全 + 深拆规范 + skill 规范补全）完成。源仍未 commit 进本地版本库（用户待定）。

## 006 笔记行文中性化修正（redbook-bio-note-writer skill 规范同步）
- **问题**：004/005/006 真实试用 + 小红书笔记出现主观/拟人句式（"这个 skill 是什么""这个 skill 教对了什么""这个 skill 不负责做比对"），读着带情感、不客观。
- **修笔记**：004/005/006 真实试用 + 小红书版逐篇替换——"这个skill是什么"→"功能定位与适用范围"、"它教的是"→"内容覆盖"、"教对了什么"→"实践要点"；修正 006 一处双冒号"内容覆盖：："。
- **修 skill 规范**：`redbook-bio-note-writer/SKILL.md` 第四节加"客观描述不拟人"纪律，8.1/8.2 模板标题改中性；commit `3630a93..d6ca401` 已 push 远程。
- 重建站点验证中性标题渲染正常（功能定位与适用范围 / 实践要点 / 内容覆盖）。

## 006「发现一/发现二」标号移除 + 「结构一致文字各异」纪律（用户反馈）
- **问题**：006 小红书/真实试用用了"发现一/发现二"标号，用户指出该句式"显得好像别人有错误一样"（带指摘味）；并明确"文章结构保持即可，不用文字都保持一致，多篇文章都是一个形式很难受"——即骨架可一致、行文须随主题自然变化，不该每篇套同一套编号/话术。
- **修笔记**：006 小红书 "## 发现一：..."/"## 发现二：..." → "## kpic-smart-gap 只动 gap 列"/"## --log 让修剪可被审计"；006 真实试用 "### 发现一实测/发现二实测" → 描述性标题（去掉"实测"标号）。
- **修 skill 规范**：8.1 模板 "### 发现一实测：<标题>" → "### <实测结果的描述性标题>";8.2 模板 "## 坑一/发现一"/"## 坑二/发现二" → "## <要点一/二的描述性标题>"、"坑一：<标题>" → 描述性要点标题示例、"一上来就坑一坑二" → "一上来就堆要点";表格"2-3 发现/核心发现" → "2-3 个实测要点";第四节新增"结构一致、文字各异"纪律（骨架一致、标题行文描述性随主题变化、不强制发现一/发现二·坑一/坑二编号）。
- **同步**：commit `d6ca401..3426104` 已 push 远程（`redbook-bio-note-writer/SKILL.md` 单文件，其余未跟踪项按"与本项目无关不动"保留）。重建站点 006 页面 26KB 正常。
- **原则固化**：同一系列保骨架（功能定位/成分拆解/严格复现/实践要点），但标题与行文不套固定编号模板，避免多篇雷同。

## 去防御性表述 + 封面去刻意规整（用户反馈）
- **问题一：防御性表述不自信**——"真实试用""真实运行 · 非演示稿"这些词在反复强调"我不是假的"，读着别扭；用户原话："你是 不自信吗？"
- **问题二：封面太刻意**——截图显示封面卡「核心能力」+ 三个规整圆点列表（● 选模式：xxx / ● 留审计：xxx / ● 保留信号：xxx）像 PPT 模板，过于刻意。
- **修 make_cover.py**：
  - 底部脚注 `'真实运行 · 非演示稿'` → 删除（默认不输出脚注，`--tag` 仅在非空且非旧默认值时才显示底部文字）。
  - `--tag` 默认值从 `'bioSkills 真实试用'` → `''`（空字符串，不输出）。
  - 「核心能力」加粗标题 + 红色粗圆点 ● → 去掉标题、圆点改轻量灰 `·`、字号颜色均降调（更自然低调）。
- **修 note-writer skill 规范**：
  - 第八节标题「真实试用深拆 + 小红书精简」→「站内深拆版 + 小红书引流版」。
  - 全文「真实试用笔记/真实试用=深拆/真实试用进站点」→「站内笔记/站内版=深拆/站内版进站点」（文件名前缀 `真实试用` 保留为技术标识，build_lab_site.py 靠它匹配）。
  - 8.1/8.2 模板 META 系列 `bioSkills 真实试用` → `bioSkills`；一级标题 `# 真实试用 bioSkills xxx` → `# bioSkills xxx`；META 标题去掉「真实试用」前缀。
  - 8.2 小红书模板「我测了」→「怎么跑的」（描述性，不带主观色彩）。
  - 封面卡生成器文档新增风格要求：--cap 用自然语句、无防御性脚注、要点用轻量分隔符。
  - 对比表表头「真实试用笔记(8.1)」→「站内笔记(8.1)」，「坑点」→「实测要点」。
- **修 6 篇笔记**：004/005/006 的真实试用版 + 小红书版，统一去掉一级标题/META/系列里的「真实试用」前缀；006 小红书「我测了」→「怎么跑的」。
- **重生成 006 封面**：用新 make_cover.py 输出（无脚注、无"核心能力"标题、要点用自然语句 + 轻量分隔符）。
- **同步**：commit `3426104..2fdec7a` 已 push 远程（note-writer skill）；站点重建正常（006 页 26KB）。

## 出图质量修复 + 封面白底化 + 副标题移除（用户反馈）
- **问题一出图重叠**：006 的 `006-fig.png` legend 文字（"Alignment column"）与 x 轴刻度数字明显重叠。根因：`make_fig.py` 没引用 `bioSkills-figure-quality` 的 `fig_quality.py` helper，legend 用 `bbox_to_anchor=(0.5, -0.28)` 硬塞图底但没留底边距。
- **修出图脚本**：`make_fig.py` 重写——import `fig_quality.py` 的 `place_legend`/`caption`/`save_clean`/`C_CORRECT`/`C_WRONG`；legend 改 `loc='outside right'` 推到 plot 区域右侧外；底部说明改 `caption(fig, ...)` 放 fig 级（x 轴标签之下）；标题精简去掉第二行冗余；配色统一用语义常量。43KB→38KB。
- **问题二封面配色不友好**：旧暖纸 #eee8db + 暗 #b5341f 在小红书信息流里显沉闷/老气；用户建议白色底。
- **修封面生成器**：`make_cover.py` 全面重写配色——纯白底 #ffffff + 暖红 #d54432（更亮、手机屏友好）；左侧细色条替代顶部粗短条；去 tag 行、去底部脚注逻辑；要点用轻量灰 `·` 分隔符。
- **问题三副标题过长**：6 篇笔记 META 副标题都是一长句（与标题/META 信息重复），用户要求尽量不用。
- **修笔记**：004/005/006 共 6 篇（真实试用+小红书）的 META `副标题:` 行全部删除。
- **修 skill 规范**：note-writer 第四节新增「副标题尽量不用」+「封面白底清爽」两条纪律；8.1/8.2 模板副标题行移除。
- **同步**：commit `2fdec7a..64bac03` 已 push 远程；站点重建正常（006-fig.png 38KB 无重叠）。
